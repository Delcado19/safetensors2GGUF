[project]
name = "safetensors2gguf"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "gguf",
    "torch",
    "safetensors",
    "tqdm",
]

[dependency-groups]
dev = [
    "pytest",
]

