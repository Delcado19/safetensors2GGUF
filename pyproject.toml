[project]
name = "safetensors2gguf"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "gguf",
    "torch",
    "safetensors",
    "tqdm",
    "gradio>=6.14.0",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff>=0.15.13",
]

