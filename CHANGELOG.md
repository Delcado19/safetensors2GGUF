# Changelog

All notable changes to this project are documented here.

Format based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added
- **Safetensors format dropdowns (Convert → Safetensors and Convert → Text Encoder) now show a size-savings percentage per format**, matching the pattern `quantize.py`'s GGUF dropdown already used (`"Q4_K_M — ... 67% smaller than F16"`). FP8/FP8_MIXED/INT8/INT8_MIXED show `~43% smaller than F16`, NVFP4/NVFP4_MIXED show `~62% smaller than F16` — hand-written literals (not computed at runtime) derived from an actual reference conversion (HiDream-I1's `llama-3.1-8b` text encoder, F16 16,060,556,312 bytes), the same one-reference-checkpoint approach `quantize.py`'s own GGUF ratios use; an approximation, not a per-architecture guarantee. New `safetensors_quant.py`'s `_SIZE_RATIOS`; `text_encoder_convert.py`'s `TEXT_ENCODER_FORMAT_CHOICES` labels mirror the same numbers. Sync tests added on both sides (mirroring `test_quantize.py`'s existing `TestAllQuantChoicesLabels` pattern) so the two dropdowns' percentages can't silently drift apart from `_SIZE_RATIOS` or from each other.

### Verified
- **`llama-3.1-8b`/`t5-xxl` (HiDream-I1's text encoders) GGUF Q4_K_M moved to `✓ Verified`**: built against the new vendored `text_encoder_configs/llama-3.1-8b/` config, loaded through ComfyUI-GGUF's `QuadrupleCLIPLoaderGGUF` alongside the other 3 HiDream encoders, render-tested clean at the same fixed seed used for the safetensors formats — composition/identity/outfit identical to the FP8 baseline, only cosmetic background-text-glyph variance. Along the way, hit and worked around a real ComfyUI-GGUF limitation: `QuadrupleCLIPLoaderGGUF` raises `NotImplementedError("Mixing scaled FP8 with GGUF is not supported!")` when any safetensors slot still carries the Comfy-Org `scaled_fp8` sentinel tensor (`nodes.py`'s `CLIPLoaderGGUF.load_data()`) — this tool's own conversions never carry that sentinel (see the `scaled_fp8` fix above), only the untouched Comfy-Org source files do, so this is a ComfyUI-GGUF-side workflow constraint, not a bug in this tool's output. `model_support.py`'s `_TE_RENDER_VERIFIED` gains `("llama-3.1-8b", "GGUF")`/`("t5-xxl", "GGUF")`. Test added.
- **`hidream` diffusion model `NVFP4_MIXED` moved to `✓ Verified`**: render-tested clean at a fixed seed (2 prompts: eclipse landscape, gothic portrait) against the already-verified `FP8` baseline — identical composition/identity/outfit, only minor secondary-detail variance (a background text glyph, a pendant's exact shape). Unlike every other `hidream` entry, this one was quantized from `hidream_i1_dev_fp8.safetensors`, not the original bf16 (no longer available locally; the user explicitly declined a third ~34GB re-download and accepted the double-quantization tradeoff). `safetensors_quant.py`'s `_RENDER_VERIFIED_MIXED` gains `("hidream", "NVFP4_MIXED")`. Test added.

### Added
- **`NVFP4`/`NVFP4_MIXED` re-added to the Convert → Safetensors dropdown** (`SAFETENSORS_DTYPE_CHOICES`, `safetensors_quant.py`): closes the "Open follow-up" from `docs/issues_analysis.md` #16 — `full_precision_nvfp4` (default on since 2026-08-13) gives NVFP4 the same safe-mode mechanism FP8 already has, and it's since been render-tested clean across `flux`/`sdxl`/`sd1`/`sd3`/`hidream`/`aura` (`_RENDER_VERIFIED_MIXED`). New `docs/issues_analysis.md` #17. Output-filename/table-click/dropdown-annotation code paths already handled NVFP4 generically once it was a real dropdown key; only stale docstrings (`gui.py`'s `apply_support_table_selection()`) needed correcting. Tests added.
- **Output filenames and dropdown labels aligned with ComfyUI/Civitai/HuggingFace naming convention**: prompted by a user question ("do we offer `fp8_e4m3fn_scaled`?") — researched how the community names these compression levels. `FP8`/`FP8_MIXED` output files are now named `<model>-fp8_e4m3fn_scaled(_mixed).safetensors` instead of `-FP8(_MIXED).safetensors` — the internal `target_key` strings are unchanged (still `"FP8"`/`"FP8_MIXED"` everywhere in code/tests), only the filename differs, via new `safetensors_quant.py`'s `filename_suffix_for()`/`_FILENAME_SUFFIX`, applied in `convert_to_safetensors()`'s own default filename and `gui.py`'s `_resolve_dst_st`/`_resolve_dst_te`. `NVFP4`/GGUF's `Q4_K_M`-style keys already matched external convention (NVIDIA's own name, llama.cpp's own naming) and were left unchanged; dropdown labels for both now also spell out that NVFP4 is NOT the same algorithm as NF4 (bitsandbytes, common on Civitai for Flux NF4 releases), which this tool doesn't offer. `docs/issues_analysis.md` #17, README updated. Tests added.
- **New `llama-3.1-8b` text-encoder family** (HiDream-I1's 4th encoder, Llama-3.1-8B-Instruct): had no `_FAMILY_SIGNATURES` entry at all before this. `text_encoder_convert.py` gains `(4096, 32, 128256): "llama-3.1-8b"`; `model_support.py`'s `TEXT_ENCODER_FAMILY_DISPLAY_NAMES` gains the family. Test added.
- **Vendored `text_encoder_configs/llama-3.1-8b/`** so this family's GGUF conversion needs no live HuggingFace download: hand-written `config.json` (verified against the checkpoint's actual tensor shapes — `hidden_size=4096`, `num_hidden_layers=32`, `num_key_value_heads=8`, `vocab_size=128256`, `rope_theta=500000.0`, `rope_scaling.rope_type="llama3"`) plus `tokenizer.json`/`tokenizer_config.json` copied from ComfyUI's bundled `llama_tokenizer/`, trimmed of the 3 `added_tokens` entries (`<unk>`=128256, `<image>`=128257, `<pad>`=128258) that overflow the checkpoint's real 128256-row embedding table — see the `Fixed` entry below for the `pad_token`/`unk_token` follow-up fix that trimming alone didn't cover. Both `llama-Q4_K_M.gguf` and `t5xxl-Q4_K_M.gguf` built successfully against it.

### Verified
- **`llama-3.1-8b`/`t5-xxl` (HiDream-I1's text encoders) FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4/NVFP4_MIXED moved to `✓ Verified`**: render-tested clean at a fixed seed via a live HiDream-I1-Dev workflow, matching the unquantized baseline's composition/identity across all 6 formats for both encoders. `model_support.py`'s `_TE_RENDER_VERIFIED` gains 12 entries (6 formats × 2 families); `t5-xxl`'s display name now also credits HiDream-I1. A separate, spatially complex prompt showed a non-monotonic subject-placement drift that did not correlate with quantization strength (documented in `docs/issues_analysis.md`'s new "Observations (not bugs)" section, not treated as a defect).

### Corrected
- **HiDream-I1 is not end-to-end quantizable — CLIP-L/CLIP-G, 2 of its 4 text encoders, share the same `quantization_metadata`-wiring gap already documented for SDXL's CLIP-L/CLIP-G**: confirmed 2026-08-18 by reading `comfy/text_encoders/hidream.py`'s `hidream_clip()` factory, which only accepts `t5_quantization_metadata`/`llama_quantization_metadata` kwargs — no CLIP-L/CLIP-G equivalent exists. F16 safetensors is the only safe format for these two encoders on HiDream-I1, same as on SDXL/FLUX.1. `model_support.py`'s existing `clip-l`/`clip-bigg` `✗ Known-issue` classification already covers this (same family signatures, shared code path); added a comment cross-referencing HiDream-I1 explicitly, plus a README correction, so "HiDream-I1's text encoders are Verified" isn't misread as covering all four.

### Fixed
- **Already-quantized checkpoints using Comfy-Org's `.scale_weight` sidecar naming (reversed word order from this tool's own `.weight_scale`) were never detected as quantized at all**: `detect_quantized_weight()`/`dequantize_weight()` (`dequantize.py`) and `_scan_quantized_layers()` (`convert_safetensors.py`) only recognized `.weight_scale`/`.weight_scale_2`/`.comfy_quant`. Found 2026-08-18 converting HiDream's `llama_3.1_8b_instruct_fp8_scaled.safetensors` (Comfy-Org's own "fp8_scaled" repackaging convention): every `.weight` tensor's raw on-disk `float8_e4m3fn` bytes got fed straight into the normal quantization pipeline unscaled — numerically wrong output for every affected layer, not just a missed optimization — and each orphaned 0-dim `.scale_weight` sidecar got processed as an ordinary weight, crashing NVFP4's `quantize_nvfp4()` on `.shape[-1]` for a shape with zero dimensions (`IndexError: tuple index out of range`). New `dequantize.py`'s `_find_scale_key()` checks both naming conventions; `_scan_quantized_layers()`'s skip-set now also excludes `.scale_weight`. Tests added (detection, round-trip value reconstruction, and the original NVFP4 crash reproduction).
- **Comfy-Org's `scaled_fp8` sentinel tensor crashed GGUF conversion of any re-quantized `fp8_scaled` checkpoint**: this 0-element `float8_e4m3fn` marker (bare key `"scaled_fp8"`, or prefixed) exists solely so `comfy/utils.py`'s `convert_old_quants()` can detect its own legacy repackaging format by *presence*, not content — it carries no real weight data. Left unstripped, it rode harmlessly through this tool's own safetensors output (ComfyUI's loader tolerates the unmapped key) but crashed `llama.cpp`'s `convert_hf_to_gguf.py` outright with `ValueError: Can not map tensor 'scaled_fp8'`. Found 2026-08-19 GGUF-converting HiDream's Llama-3.1-8B/T5-XXL text encoders (both shipped as Comfy-Org `fp8_scaled` checkpoints). `convert_safetensors.py`'s `_scan_quantized_layers()` skip-set now also excludes any key ending in `scaled_fp8`. Test added.
- **Llama-3.1-8B GGUF conversion asserted `max(tokenizer.vocab.values()) < vocab_size`**: HF's `AutoTokenizer` re-appends `pad_token`/`unk_token` at fresh out-of-range IDs whenever the token they name (`<pad>`, `<unk>`) isn't already present in the loaded vocab — independent of what `tokenizer.json`'s `added_tokens` or `tokenizer_config.json`'s `added_tokens_decoder` list, and regardless of trimming either of those to the checkpoint's real 128256-row embedding table. Found 2026-08-19 building the new `llama-3.1-8b` vendored config (see below): `pad_token` repointed to the in-vocab `<|end_of_text|>`, `unk_token` set to `null`.

### Added
- **Convert Text Encoder tab gained a "Browse" button for the output path** (`te_dst_path`), matching the GGUF and Safetensors tabs. New `browse_and_set_dst_te()`/`_resolve_dst_te()` in `gui.py` mirror the existing `browse_and_set_dst_st()`/`_resolve_dst_st()` pair, but pick `.gguf` vs `.safetensors` from `TEXT_ENCODER_SAFETENSORS_FORMATS` since this tab's single format dropdown spans both extensions (the other two tabs each only ever produce one). This also fixes a real bug, not just a missing button: `run_te_convert()` previously passed a hand-typed `dst_path` straight through with no directory-vs-file resolution, so a bare directory path (the natural thing to type without a save dialog) reached `llama-quantize` as an output-file target and failed with a bare C++ `ios_base::failbit set: iostream stream error` instead of a clear message — confirmed 2026-08-18 converting `aura_t5` to Q4_K_M. `_resolve_dst_te()` now appends `<source-stem>-<format><ext>` when given a directory (trailing slash or an existing dir), same as the other two tabs already did. Tests added.

### Documented
- **`pile-t5xl` (AuraFlow's `aura_t5`) FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4/NVFP4_MIXED moved to `✗ Bad`**: render-tested via the verified `aura_flow_0.3-NVFP4_MIXED` diffusion model. FP8/INT8 both produced complete full-image structured noise; NVFP4_MIXED crashed outright (`mat1 and mat2 shapes cannot be multiplied (256x2048 and 1024x2048)` — NVFP4's last-dim packing halving 2048 to 1024). Not a bug in this tool's output: `comfy/text_encoders/aura_t5.py`'s `AuraT5Model` never got `*_quantization_metadata` wiring added upstream at all (unlike `flux_clip`/`sd3_clip`/`hidream_clip`/`lumina2`/`qwen_image`, which all accept and route their own quantization-metadata kwarg) — structurally unsupported by ComfyUI, same class of gap as `clip-l`/`clip-bigg`. Only F16 is safe among the safetensors formats for this text-encoder family. `model_support.py`'s `_TE_RENDER_CONFIRMED_BAD` gains all four `pile-t5xl` entries; new family added to `TEXT_ENCODER_FAMILY_DISPLAY_NAMES`. Test added.
- **`pile-t5xl` GGUF Q4_K_M moved to `✓ Verified`**: render-tested clean across 2 motifs via both an F16 and an FP8_MIXED `aura_flow_0.3` diffusion model, matching the unquantized baseline exactly. GGUF loads through ComfyUI-GGUF's own `CLIPLoaderGGUF` node, independent of the `quantization_metadata` gap that makes every safetensors-quant format `✗ Bad` for this family above — a real, working alternative. `model_support.py`'s `_TE_RENDER_VERIFIED` gains `("pile-t5xl", "GGUF")`. Test added.

### Corrected
- **Docs overstated `flux` render-test coverage**: every render-test backing the `flux` arch_key's `✓ Verified` status (in `safetensors_quant.py` and `model_support.py`) used FLUX.2 Klein 9B checkpoints (qwen3-8b text encoder) exclusively — Flux.1/Flux.1 Kontext (T5-XXL text encoder) share the same `arch_key` via `ModelFlux`'s tensor-naming detection but have never themselves been render-tested. Added explicit disambiguation comments in both files so "flux is VERIFIED" isn't misread as covering Flux.1. `t5-xxl`'s text-encoder support level was already `? Unknown` (unaffected, no code change there).

### Verified
- **`aura` (AuraFlow) — F16/F16_MIXED/FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4_MIXED/Q4_K_M GGUF moved to `✓ Verified`**: render-tested clean via `aura_t5` (Pile-T5-XL), fixed seed, matching the F16 baseline's composition/identity across 4 motifs (including the original close-up vampire-portrait prompt that first showed drift). NVFP4/NVFP4_MIXED initially showed a reproducible composition/lighting drift, root-caused to the two bugs fixed directly below; NVFP4_MIXED re-converted and re-render-tested clean after both fixes. Plain NVFP4 moved to `⚠ Caution`, not `✓ Verified`: `keys_hiprec` only ever protects `*_MIXED` by design (same tradeoff every other architecture has), and on the vampire-portrait prompt specifically it still showed visible-but-tolerable facial-detail loss (no wrong identity, no crash) vs. the baseline — the 3 other, less face-detail-dependent motifs matched cleanly. `safetensors_quant.py`'s `_RENDER_VERIFIED_MIXED` gains `("aura", "FP8")`/`("aura", "FP8_MIXED")`/`("aura", "INT8")`/`("aura", "INT8_MIXED")`/`("aura", "NVFP4_MIXED")`; `model_support.py`'s `_RENDER_TESTED_DRIFT` gains `("aura", "NVFP4")`. Tests added.

### Fixed
- **`is_hiprec_st()`/`_quant_type_for()` silently made `keys_hiprec` a no-op for F16-native checkpoints**: both functions only granted hiprec protection when a tensor's source dtype was `float32`/`bfloat16` — any checkpoint shipping natively as `float16` (confirmed for `aura_flow_0.3`, unlike the `bfloat16`-native Flux/SD3/HiDream/Lumina2 checkpoints this mechanism was validated against) got every `keys_hiprec` entry quantized anyway in every `*_MIXED` format, since the dtype gate excluded it before the `keys_hiprec` substring check ever ran. Found 2026-08-18: adding `ModelAura.keys_hiprec` (see directly below) had zero effect on a re-render until this was also fixed. `safetensors_quant.py`'s `is_hiprec_st()`, `convert.py`'s `_quant_type_for()` (the GGUF-side mirror), and `safetensors_quant.py`'s `estimate_safetensors_output_size()` (the GUI size-estimate mirror) now all also accept `float16` sources. Tests added (`is_hiprec_st`/`quantize_tensor_st` with an explicit F16 source).
- **`ModelAura` had no `keys_hiprec` at all**: the AdaLN modulation Linears (`modC`/`modX`/`modCX`/`modF` — produce the per-block scale/shift/gate applied to the *entire* residual stream, same tensor class already protected for `ModelFlux`/`ModelLumina2`) plus the input/time embedders and final output projection were quantized in every format, including `NVFP4_MIXED`, which should have been immune. `models/architectures.py`'s `ModelAura` now sets `keys_hiprec = ["modC", "modX", "modCX", "modF", "final_linear", "init_x_linear", "t_embedder", "cond_seq_linear"]`.

### Fixed
- **T5/UMT5 text encoders' `relative_attention_bias.weight` crashed loading in ComfyUI under NVFP4/NVFP4_MIXED**: this small `[num_buckets, num_heads]` lookup table is read via a bare `nn.Embedding` indexing op (`comfy/text_encoders/t5.py`'s `T5Attention.forward`), the same dequant-bypass class of bug as CLIP's `position_embedding`/`text_projection`. NVFP4 halves the on-disk last dimension, so HiDream's T5-XXL (`[32, 64]`) shrank to `[32, 32]` on disk and failed `load_state_dict` with `size mismatch ... torch.Size([32, 32]) ... current model is torch.Size([32, 64])`. Found 2026-08-18 render-testing HiDream's quantized T5-XXL encoders. `text_encoder_convert.py`'s `_TEXT_ENCODER_MODEL_ARCH.keys_shape_critical` gains `"relative_attention_bias"`, forcing F16 for this tensor in every format (FP8/INT8/NVFP4, plain and `_MIXED`). Test added.
- **`hidream` plain NVFP4 crashed on load and plain INT8 produced severely corrupted output**: `ModelHiDream.keys_hiprec` already listed `.ff_i.gate.weight` (the MoE router) and `img_emb.emb_pos`, but that list only skips quantizing a tensor in `_MIXED` formats — plain formats quantize everything, including hiprec tensors, by design. That's normally fine (a quality/risk tradeoff the user opts into), but this specific tensor never goes through ComfyUI's `MixedPrecisionOps` quantized-loading path at all — it's read via a raw `state_dict` assign, the same bug class as `safetensors_quant.py`'s FP8-branch CLIP `position_embedding` example. NVFP4 halving `ff_i.gate.weight`'s last dim (`[4, 2560]` → `[4, 1280]` on disk) broke `load_state_dict`'s shape check outright (`size mismatch ... [4, 1280] ... [4, 2560]`, confirmed against a live ComfyUI HiDream-I1-Dev render); plain INT8 quantized the same tensor and produced a structurally collapsed image, not just quality drift. `models/architectures.py`'s `ModelHiDream` now also sets `keys_shape_critical = [".ff_i.gate.weight", "img_emb.emb_pos"]`, which forces F16 for these tensors in every quantization branch (FP8/INT8/NVFP4) regardless of plain/mixed — the same mechanism already used for Flux/SD3/SDXL's shape-inference bugs, applied here for a different reason (raw-load bypass, not shape-based hyperparameter inference). `FP8_MIXED`/`INT8_MIXED` were unaffected (already protected via `keys_hiprec`) and render-tested clean; tests added.
- **CI: `TestGgufUnsupportedFamilies` failed on a clean checkout with no local llama.cpp clone**: `convert_text_encoder()` called `find_convert_script()` (which auto-clones llama.cpp via subprocess if not already present) before `_reject_if_gguf_unsupported()`. Locally this was masked because llama.cpp was already cloned, so the clone subprocess was a no-op; in CI (no cached clone, restricted network) the clone itself failed first with an unrelated `ValueError`, hiding the intended `RuntimeError` for CLIP families. `text_encoder_convert.py` now runs the (cheap, offline) family check before `find_convert_script()`.
- **`sd3` FP8/FP8_MIXED/NVFP4/NVFP4_MIXED moved from `? Unknown` to `✓ Verified`** (INT8/INT8_MIXED were already `✓ Verified` via the `keys_hiprec`-empty fast path): render-tested clean via CLIP-G — F16/F16_MIXED/FP8/FP8_MIXED/INT8/INT8_MIXED/Q4_K_M GGUF confirmed first, then NVFP4/NVFP4_MIXED after the shape-critical fix directly below, all matching the F16 baseline's composition/identity with no visible deviation. `safetensors_quant.py`'s `_RENDER_VERIFIED_MIXED` gains four `sd3` entries; `model_support.py`'s docstring updated; test added.
- **`sd3` NVFP4/NVFP4_MIXED crashed on load** (`RuntimeError: mat1 and mat2 shapes cannot be multiplied`, confirmed against a live ComfyUI SD3-medium render): `ModelSD3.keys_shape_critical` only listed `context_embedder.weight`, missing `y_embedder.mlp.0.weight` — the same pattern already fixed for Flux/SDXL (`docs/issues_analysis.md` #9). ComfyUI's `model_detection.py` reads that tensor's raw on-disk `shape[1]` to infer `adm_in_channels`; NVFP4 halves the last dim during packing, so the model got built with a corrupted (halved) `adm_in_channels`, breaking the shape of `y_embedder.mlp`'s first Linear layer. `models/architectures.py`'s `ModelSD3.keys_shape_critical` now also lists `y_embedder.mlp.0.weight`; test added.

### Corrected
- **`sd1` FP8/FP8_MIXED/NVFP4/NVFP4_MIXED moved from `? Unknown` to `✓ Verified`** (INT8/INT8_MIXED were already `✓ Verified` via the `keys_hiprec`-empty fast path, same as `sdxl`): render-tested clean on the first try — DreamShaper 8 (civitai.com/models/4384) via CLIP-L across 2 prompts (a photoreal portrait, a fantasy character portrait), matching the F16 baseline's composition/identity/outfit with only quantization-noise-level decorative variance. No code changes needed: `sdxl`'s earlier fixes this session (the `dim()>=3` Conv2d guard, `ModelSD1.keys_shape_critical`'s `attn2.to_k.weight` entry) already covered `sd1`, and SD1 has no class-conditioning `label_emb` to need an analog of that other `sdxl` fix. `safetensors_quant.py`'s `_RENDER_VERIFIED_MIXED` gains four `sd1` entries; `model_support.py`'s docstring updated.

### Added
- **`convert_to_safetensors()` warns when quantization makes the output *larger* than the input**: per-layer scale/zero-point tensors added during quantization can outgrow the savings on small or oddly-shaped models, silently producing a file that is both bigger AND lower precision than the source — no code path caught that contradiction before. Now compares `os.path.getsize()` of `dst_path` vs. the source `path` after `save_file()` and logs a `WARNING:` line (via the existing `on_log` callback) when the output regresses; the conversion itself still succeeds. Two tests added (`test_warns_when_output_is_larger_than_input`, `test_no_warning_when_output_shrinks`).
- **Text Encoder Support table gains an F16 column**: previously had none at all (only GGUF, which collapses every GGUF outtype/K-quant, and the six quantized safetensors formats) — a family like `clip-l`/`clip-bigg`, where plain F16 safetensors is the *only* format that actually works (see the CLIP quantization entry below), showed a row of nothing but `✗` with no visible sign that anything works. `model_support.py`'s `TEXT_ENCODER_TABLE_FORMATS` gains `("F16", "F16")`; `text_encoder_support_level()` returns `✓ Verified` for it unconditionally, mirroring the diffusion-model table's F16/F16_MIXED columns (a plain precision cast never touches ComfyUI's quantized-compute path). Also fixes two places this new column's key needed translation, not a direct match, since `"F16"` was already taken by the GGUF outtype: `gui.py`'s support-table click handler (`apply_text_encoder_support_table_selection`) now maps the table's `F16` column to the dropdown's `F16_ST` key, and `annotate_text_encoder_choices()` maps `F16_ST` back to the `F16` table column for the ✗/⚠/? dropdown annotations.
- **Convert Text Encoder tab's format dropdown now warns dynamically** (✗/⚠/? prefix, mirroring the diffusion-model Safetensors tab's `_annotate_choices_for_arch`) as soon as a source file or base-repo-ID resolves to a known family — e.g. loading a CLIP-G checkpoint immediately marks every GGUF-family entry `✗`, before the user even clicks Convert. Only ever adds a warning prefix, never hides/disables an option — Gradio's `Dropdown` has no disabled-choice concept. `gui.py` adds `annotate_text_encoder_choices()`/`_detected_text_encoder_family()`, wired to both `te_src_path` and `te_base_repo`'s `.change()`.

### Corrected
- **CLIP-L/CLIP-G's FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4/NVFP4_MIXED moved from `? Unknown` to `✗ Known issue`** (only plain safetensors F16 stays viable, alongside the already-`✗` GGUF entries): after the `position_embedding`/`text_projection` fixes below, `clip_g` FP8 still rendered solid black and NVFP4 still crashed. Root cause is a genuine ComfyUI-side gap, not fixable from this tool: `comfy/sd1_clip.py`'s `CLIPTextModel.__init__()` only selects the quantization-aware `MixedPrecisionOps` when `model_options["quantization_metadata"]` is set, and `comfy/sd.py`'s `load_text_encoder_state_dicts()` only ever sets that key inside its `CLIPType.MINIMAX` branch — the standard `TEModel.CLIP_G`/`CLIP_L` path (every SDXL load) always falls through to plain `comfy.ops.manual_cast`, which has no `.comfy_quant`/`weight_scale`-aware loading at all. Confirmed this tool's own `.comfy_quant` sidecar synthesis works correctly (`comfy.utils.convert_old_quants()` reads our file-level `_quantization_metadata` and reconstructs the sidecar exactly as designed) — it's just never consulted for CLIP-L/CLIP-G, regardless of how correct the output file is. `model_support.py`'s `_TE_RENDER_CONFIRMED_BAD` gains 12 entries (both families × 6 formats); two tests updated/added.

### Fixed
- **CLIP-G's `text_projection` was also unprotected**: after the `position_embedding` fix below, `clip_g` FP8 still rendered solid black. `text_projection.weight` is the *only* weight feeding SDXL's global pooled `y` conditioning vector (`adm_in_channels=2816`) — CLIP-L contributes no pooled/projected output to SDXL at all (its extracted file has no `text_projection.weight`), only per-token hidden states. Unlike `position_embedding`, `comfy/clip_model.py` calls this as a proper module (`self.text_projection(x[2])`), so it should in principle dequantize correctly — but as a single global vector (no per-token error-averaging the way attention has), it's uniquely exposed to quantization noise skewing the *entire* image rather than just local detail. Added to `_TEXT_ENCODER_MODEL_ARCH.keys_shape_critical` alongside `position_embedding`; regression test added; clip_g and clip_l re-converted (source checkpoint changed mid-session to `wildcardxXLFusion_fusionOG`, all 6 formats × both encoders regenerated fresh).
- **CLIP-L/CLIP-G's `position_embedding` was unprotected under FP8/INT8/NVFP4, crashing (NVFP4) or silently corrupting (FP8/INT8) every render**: `comfy/clip_model.py`'s `CLIPEmbeddings.forward()`/`CLIPTextModel_.forward()` both read `self.embeddings.position_embedding.weight` via a bare attribute access (`comfy.ops.cast_to(...)`), not a module call — bypasses the `Embedding` class's quantized-dequant machinery entirely. NVFP4 crashed outright (`RuntimeError: The size of tensor a (768) must match the size of tensor b (384)` — NVFP4 halves the on-disk last dim); FP8/INT8 quantized without error but produced numerically wrong (raw quantized bytes, never dequantized) output, visible only as a corrupted/black render. Found a second, broader bug while fixing this: unlike NVFP4/INT8, the **FP8 branch never checked `keys_shape_critical` at all** — invisible until now because FP8 never corrupts on-disk *shape* (the failure mode `keys_shape_critical` was originally built for), but `position_embedding` needed *value* protection instead, which FP8 skipped just as much as shape-changing formats do. `text_encoder_convert.py`'s `_TEXT_ENCODER_MODEL_ARCH.keys_shape_critical` gains `"position_embedding"`; `safetensors_quant.py`'s FP8 branch gains the same `keys_shape_critical` guard NVFP4/INT8 already had (architecture-agnostic — this could have silently affected any architecture's FP8 output, not just CLIP text encoders, for whatever shape-critical keys each one declares). Two regression tests added. All 12 of SDXL's clip_g/clip_l FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4/NVFP4_MIXED files re-converted with the fix.
- **CLIP-L/CLIP-G → GGUF now fails fast with a clear explanation instead of llama.cpp's opaque `Model CLIPModel is not supported`**: found converting SDXL's `clip_g` to F16 GGUF, which ran the full config-fetch + subprocess dance before failing. `convert_hf_to_gguf.py` has no CLIPModel/CLIPTextModel converter at all (confirmed by grepping the vendored `.llama.cpp` checkout — zero matches), and ComfyUI-GGUF's `CLIPLoaderGGUF` node has no CLIP-L/bigG decode path either, so this isn't "untested" or "one outtype might work" — every GGUF outtype and K-quant is structurally impossible for these two families. `text_encoder_convert.py` adds `_reject_if_gguf_unsupported()`, checked both on auto-detected family and on a manually-typed `base_repo_id` matching a vendored CLIP repo, before any config fetch or subprocess call. `model_support.py`'s `_TE_RENDER_CONFIRMED_BAD` gains `("clip-l", "GGUF")`/`("clip-bigg", "GGUF")` so the support table reflects this too (a deliberate stretch of BAD's usual "render-tested and confirmed broken" meaning — this never gets far enough to render — but more useful than UNKNOWN, which would invite retesting something that can never succeed).

### Added
- **Convert Text Encoder tab can now output plain F16 as a real `.safetensors` file**, not only GGUF: the existing "F16" dropdown entry is a GGUF outtype (unchanged), and a new **"F16 (safetensors)"** entry (GUI key `F16_ST`, translated to the real `F16` `safetensors_quant` target key before dispatch — see `_TEXT_ENCODER_SAFETENSORS_TARGET_KEY`) routes through `convert_text_encoder_to_safetensors()` instead and writes a real `.safetensors` file. First attempt reused the existing `F16_MIXED` key directly, which leaked into the per-tensor log (`-> F16_MIXED`) and the default output filename (`...-F16_MIXED.safetensors`) — confusing for an option labeled plain "F16" — so it was swapped for the dedicated `F16_ST` translation before this ever reached the dropdown. Good thing: a same-checkpoint test after this entry shipped (`Josiefied-Qwen3-4B-abliterated-v2`, see the BF16-instability entry below) confirmed **F16_MIXED still renders corrupted output** — it casts every 2D+ tensor to real F16 but leaves the 145 1D bias/norm tensors (incl. RMSNorm scale weights) at their original BF16, and that alone reproduces the corruption. So the instability isn't only in the large attention/MLP weight matrices; the small norm/bias tensors carry it too. `F16_MIXED` is deliberately NOT exposed in the dropdown for this reason. `text_encoder_convert.py`'s `TEXT_ENCODER_SAFETENSORS_FORMATS`/`TEXT_ENCODER_FORMAT_CHOICES` gain the new `F16_ST` entry; regression test added.
- **Convert Text Encoder tab's format dropdown regrouped GGUF-first, then safetensors** (previously interleaved, e.g. the new F16 safetensors entry sat between the two GGUF F16/BF16 entries), with every label now tagged `(GGUF)`/`(safetensors)` for unambiguous grouping at a glance. Gradio 6.x's `Dropdown` has no native separator/disabled-option support (checked the installed version's `Dropdown.__init__` signature) — a fake separator entry would be selectable and error out downstream, so none was added; the ordering itself now matches the dispatch split (`TEXT_ENCODER_SAFETENSORS_FORMATS`/`LLAMA_QUANT_KEYS`/`_GGUF_DIRECT_OUTTYPES` in `convert_text_encoder_any()`), audited entry-by-entry to confirm every dropdown key still resolves to exactly one conversion backend with none missing or orphaned.
- **`detect_arch()`'s "Unknown model architecture" error now hints at the Convert Text Encoder tab**: that error only ever fires from the diffusion-model pipeline (Convert Safetensors/GGUF tabs), so a standalone text-encoder checkpoint always trips it — the message now says so instead of just listing the (irrelevant) diffusion architectures it checked.

### Fixed (root cause: user checkpoint, not this tool)
- **`Goekdeniz-Guelmez/Josiefied-Qwen3-4B-abliterated-v2` rendered corrupted/noisy output as a Z-Image/Lumina2 text encoder despite passing every file-integrity check** (tensor count/shapes/dtype, no NaN/Inf, byte-exact re-download comparison against the HF source, correct `CLIPLoader` node/type). Root-caused by comparing a third-party F16 GGUF of the same repo (mradermacher) against our merged BF16 safetensors: dequantized GGUF tensor values matched ours to within normal BF16→F16 rounding error (max abs diff ~0.05–0.1 on 6 sampled tensors spanning embeddings/attention/MLP) — proving the *weights* are identical, so the corruption isn't in `hf_download.py`'s merge (already independently confirmed byte-exact) or in the checkpoint's values. The actual cause: this specific fine-tune's weights are numerically unstable under BF16 compute (its native/on-disk dtype) but stable under F16 (more mantissa bits) — GGUF happened to sidestep the issue only because the third-party quantizer chose an F16 target, not because GGUF format itself is special. Converting the safetensors file to **F16** with this tool's own Convert Text Encoder tab (a precision cast, no architecture/quantization changes) fixed it — confirmed clean render, and confirmed reproducible on a fresh re-download (ruling out a corrupted local copy). A follow-up test narrowed it further: **F16_MIXED** (which leaves 1D bias/norm tensors, including RMSNorm scale weights, at their original BF16) still rendered corrupted — only casting *every* tensor to real F16, not just the large attention/MLP weight matrices, fixes it. Not a bug to fix in code: documenting here because any BF16-native HF checkpoint downloaded via the "Download from HF" tab could hit the same failure mode, and "convert to plain F16, not F16_MIXED" is the known fix if a freshly-downloaded text encoder renders garbage despite passing integrity checks.

### Corrected
- **`sdxl` FP8/FP8_MIXED/NVFP4/NVFP4_MIXED moved from `? Unknown` to `✓ Verified`** (INT8/INT8_MIXED were already `✓ Verified` via the `keys_hiprec`-empty fast path): render-tested across 2 prompts (a fantasy-render composition, an unrelated photorealistic portrait) on `realvisxlV50_v50LightningBakedvae`, post the Conv-weight and `label_emb.0.0.weight` fixes below — every format matched the F16 baseline's composition/identity/outfit with only quantization-noise-level decorative variance, the same bar every other `✓ Verified` entry in this table is held to. `safetensors_quant.py`'s `_RENDER_VERIFIED_MIXED` gains four `sdxl` entries; `model_support.py`'s docstring and two tests updated.

### Added
- **"Extract Components" tab can now also pull out the diffusion model (UNet) itself**, via the same GGUF or Safetensors conversion the Convert tabs use, written straight to `diffusion_models/` (load with `UNETLoader`) instead of the checkpoint's own folder — previously the tab only extracted VAE/CLIP-L/CLIP-G, so getting a standalone diffusion model out of a checkpoint meant running the Convert tab manually and then moving the output file by hand. `gui.py` adds `_diffusion_models_dst_dir()`, `run_extract_diffusion_gguf()`, `run_extract_diffusion_safetensors()` (thin wrappers around the existing `run_convert`/`run_st_convert` pipelines — no new conversion logic) and two buttons in the tab.

### Fix
- **FP8/INT8 quantized Conv1d/2d/3d weights (any tensor with `dim() >= 3`), producing black-image renders**: real bug found render-testing SDXL FP8/INT8 (`realvisxlV50_v50LightningBakedvae`, all four F16/F16_MIXED variants render fine, all four FP8/FP8_MIXED/INT8/INT8_MIXED variants produced solid black output). Root cause: ComfyUI's `MixedPrecisionOps` (`comfy/ops.py`) implements quantized, `weight_scale`-aware loading only for `nn.Linear`/`MoEExperts`/`Embedding` — `Conv1d`/`Conv2d`/`Conv3d` have no equivalent override and fall through to the plain cast-only ops class, which never reads a `weight_scale` sidecar. `quantize_tensor_st()`'s FP8 branch had no dimension check at all, and INT8's `dim() != 2` branch routed conv weights to tensorwise quantization (still with a scale sidecar) instead of skipping them — both wrote bytes ComfyUI silently misreads as raw floats. Confirmed against a real SDXL checkpoint: `input_blocks.0.0.weight` ([320,4,3,3], a Conv2d kernel) quantized to `F8_E4M3` without any error here, but loading it as-is in ComfyUI collapsed the render to black. NVFP4 was already accidentally safe (a 3x3 kernel's last dim isn't %16, so `quantize_nvfp4` raises and falls back to F16) — this is architecture-agnostic, not SDXL-specific, since no model family's Conv layers are quantization-loadable in ComfyUI today. `safetensors_quant.py` adds an unconditional `data.dim() >= 3 → F16` guard before the format branches (and a matching case in `estimate_safetensors_output_size`); two new regression tests (`test_fp8_conv_weight_stays_f16`, `test_int8_conv_weight_stays_f16`). Re-converted SDXL's FP8/FP8_MIXED/INT8/INT8_MIXED with the fix, ready for re-testing.
- **`ModelSDXL.keys_shape_critical` was still missing `label_emb.0.0.weight`** after the `attn2.to_k.weight` fix above: ComfyUI's `model_detection.py` reads this tensor's raw `.shape[1]` to infer `adm_in_channels` (2816 for stock SDXL); NVFP4 halved it on disk (→1408), which matched no `supported_models` entry and crashed a real NVFP4/NVFP4_MIXED SDXL diffusion model on load (`'NoneType' object has no attribute 'quant_config'`, `comfy/model_detection.py:1273` — `model_config_from_unet_config()` returned `None`). Same failure class as the Flux `txt_in.weight`/`vector_in.in_layer.weight` bug; `keys_detect` already listed this key (so its shape mattered to detection) but the shape-critical protection list didn't cover it. New regression case in `test_nvfp4_shape_critical_protection_per_architecture`.
- **Closed the `ModelSDXL`/`ModelSD1` "NOT AUDITED for keys_shape_critical" gap flagged since #9**: ComfyUI's `model_detection.py` infers SDXL/SD1's `context_dim` by dynamically scanning for the first `input_blocks.N.1.transformer_blocks.0.attn2.to_k.weight` it finds (`calculate_transformer_depth()`), rather than reading one fixed key the way the DiT architectures do — the code already carried a comment flagging this as unresolved. Confirmed against a real SDXL checkpoint that every block's `attn2.to_k.weight` shares the same on-disk `context_dim` (`input_blocks.4/5/7` all `[*, 2048]`), so a substring match protects whichever block the dynamic scan actually reads without needing to pin the exact index. `models/architectures.py` adds `keys_shape_critical = ["attn2.to_k.weight"]` to both `ModelSDXL` and `ModelSD1`; two new regression cases in `test_nvfp4_shape_critical_protection_per_architecture`. Converted a real SDXL checkpoint (`realvisxlV50_v50LightningBakedvae`) to FP8/FP8_MIXED/NVFP4/NVFP4_MIXED with this fix active, ready for render-testing — `sdxl`'s FP8/FP8_MIXED/NVFP4/NVFP4_MIXED columns are still `? Unknown` pending that test.

### Corrected
- **`lumina2` NVFP4/NVFP4_MIXED moved back from `⚠ Caution` to `✗ Known issue`, reverting the same day's earlier promotion**: the CAUTION classification (see the `_RENDER_TESTED_DRIFT` entry below) was based on "2 of 3 tested prompts matched baseline cleanly" — but on review, when the 3rd prompt did fail, it failed exactly the same way plain FP8/INT8 already do (full composition/pose/outfit swap, not a minor detail), and both of those were classified `✗ Known issue` off a *single* failing test each with no "but other tests were clean" exception. Holding NVFP4 to a looser bar than FP8/INT8 for producing the identical failure mode was inconsistent, not a genuine severity difference — a format that swaps in a completely different image on some real prompts is "confirmed wrong", regardless of how often it happens to succeed elsewhere. `model_support.py` moves `("lumina2", "NVFP4")`/`("lumina2", "NVFP4_MIXED")` back to `_RENDER_CONFIRMED_BAD`; `_RENDER_TESTED_DRIFT` is empty again (the CAUTION/UNKNOWN state split it introduced stays valid, just has no current member).

### Fix
- **Download from HF: two bugs found testing `Lockout/qwen3-4b-heretic-zimage`, a repo that ships two complete checkpoints (`.../qwen-4b-zimage-heretic/` and `.../qwen-4b-zimage-hereticV2/`) side by side, each in its own subfolder**:
  1. `download_repo_as_single_safetensors()` grabbed every `.safetensors` file anywhere in the repo, so it downloaded *both* variants' shards and then hit its own "duplicate tensor key" guard at merge time (both variants share tensor key names) — wasted bandwidth and a guaranteed failure, not a real ambiguity in the merge logic. `hf_download.py` adds `_find_shard_group()`: groups shards by containing folder, auto-picks when exactly one folder has `.safetensors` files, and raises a clear `RuntimeError` naming the available subfolders when more than one does, instead of grabbing everything. New `subfolder` parameter on `download_repo_as_single_safetensors()`, wired to a new optional "Subfolder" field on the Download from HF tab. Output filename now derived from the chosen subfolder (falling back to the repo name at root) so two variants downloaded one after another don't overwrite each other's output.
  2. Cancelling or hitting an error deleted the temp shard folder unconditionally (`finally: shutil.rmtree(...)`), so a retry always restarted from zero even though `hf_hub_download`'s own `.incomplete`-file resume logic would otherwise pick up where it left off. Cleanup now only runs after a full successful merge+write; a cancelled or failed run leaves its partial shards in place so re-running with the same repo ID/subfolder resumes instead of re-downloading everything (cancellation still only takes effect between shards, not mid-transfer, since a single `hf_hub_download()` call can't be interrupted once it's streaming).
  Also flagged in conversation: the Log/Status Textboxes on this (and every other conversion) tab only reflect a *live* `run_hf_download()` generator — the actual download itself runs to completion in a background daemon thread regardless of whether the browser is still watching it, so a page reload or dropped connection loses the live view (status resets to "Ready", empty log) without affecting the download in progress. Not new to this tab; inherent to the existing `_run_job`/`_stream` streaming pattern shared by every tab. Not fixed (would need a persistent job store), just documented here since it looked like a bug in this session.

### Fix
- **`lumina2` (Z-Image) and `flux` GGUF F16/Q6_K and safetensors F16_MIXED render-confirmed, closing a documented evidence gap**: GGUF and F16/F16_MIXED were always marked `✓ Verified` for every architecture on design-reasoning grounds alone (explicit architecture detection + a precision cast that never touches ComfyUI's quantized-compute path), not per-architecture render tests — flagged in conversation as a real gap for `flux`/`lumina2` specifically, since llama-quantize's K-quant path has no `keys_hiprec` equivalent (`quantize.py`'s F32-forcing is purely size/dtype-based, nothing architecture-aware), so an architecture-dependent K-quant defect wasn't ruled out by design. 5-seed/2-prompt render test (`E:\AI_Art\FormatTest-ZImageBase`, Qwen3-4B-family text encoder) against the unquantized baseline found no defect in GGUF F16, GGUF Q6_K, or safetensors F16_MIXED — a "3 women vs. 2 women" difference between one prompt's two batch images looked like a Q6_K-specific composition defect until the *unquantized baseline itself* turned out to show the identical split, confirming prompt-adherence seed variance rather than quantization corruption. Same day, `flux` got the same render test (`E:\AI_Art\FormatTest-FluxKlein9b`, qwen3-8b vanilla text encoder, 3 prompts x 2 seeds): GGUF F16, GGUF Q6_K, and safetensors F16_MIXED all matched the baseline with no visible deviation at all — closer agreement than lumina2's, no seed-variance false alarm this time. No `model_support.py` logic changed (both were already unconditionally `SUPPORT_VERIFIED`); `support_level()`'s docstring now documents the real evidence backing that verdict for both architectures instead of just the design argument.

### Added
- **`hf_xet` dependency**: repos with Xet Storage enabled (increasingly common on HuggingFace) were silently falling back to plain HTTP downloads with a warning on every file — found while using the new Download from HF tab, where a Xet-backed repo hit a read timeout mid-shard that `huggingface_hub`'s own retry/resume handled fine, but a chunked Xet transfer would likely have avoided reconnecting a multi-GB stream from a much earlier offset in the first place. No code changes needed: `huggingface_hub` auto-detects and uses `hf_xet` when installed.
- **New "⇩ Download from HF" tab**: enter a HuggingFace repo ID and a target folder, click Download — the tool lists every `.safetensors` file in the repo, downloads all of them (including multi-shard checkpoints split as `model-NNNNN-of-NNNNN.safetensors`) into a throwaway temp folder, merges every tensor into a single dict, writes one `<repo-name>.safetensors` file, then deletes the shards. Callers only ever see the merged single-file output, matching what the Convert tabs expect as input. New `hf_download.py` module (`download_repo_as_single_safetensors()`), wired into `gui.py` following the existing worker-thread + cancel_event + `_stream` pattern shared by the other conversion tabs (dedicated `_active_cancel_hf` slot). Raises `RuntimeError` if the repo has no `.safetensors` files or two shards define the same tensor key, `OSError` if the output already exists and overwrite is off.

### Fix
- **`lumina2` NVFP4/NVFP4_MIXED moved from `✗ Known issue` to `⚠ Caution` after re-testing with the `full_precision_nvfp4` fix**: the prior `_RENDER_CONFIRMED_BAD` verdict was from a pre-fix conversion showing full-image noise (`docs/issues_analysis.md` #15). Z-Image Base was reconverted with the same `full_precision_nvfp4`/`ModelFlux`-class fix that cleared `flux` (Z-Image Base's own `keys_shape_critical` — `cap_embedder.1.weight`, `x_pad_token`, `cap_pad_token` — was audited against ComfyUI's `model_detection.py` Lumina2/Z-Image branch and found to already cover every raw shape read there, so no equivalent `txt_in.weight`-style gap exists), then render-tested 2026-08-13 with 3 prompts x 2 seeds via the `qwen3-4b` text encoder. No more garbage/noise on any seed — a real improvement over the pre-fix evidence. 2 of 3 prompts matched the BF16 baseline closely (fine-detail-only variance). The third and longest prompt showed a full composition/pose/outfit change from baseline on both NVFP4 and NVFP4_MIXED, with more variance between the two images of the same batch than baseline showed for the same prompt — real, non-trivial output deviation, but not the "wrong/broken" bar `_RENDER_CONFIRMED_BAD` requires. `model_support.py` moves `("lumina2", "NVFP4")`/`("lumina2", "NVFP4_MIXED")` from `_RENDER_CONFIRMED_BAD` to `_RENDER_TESTED_DRIFT`.

### Fix
- **`flux` NVFP4/NVFP4_MIXED render-verified clean after fixing the two bugs that caused 2026-08-12's visible composition drift**: re-tested (2026-08-13) against the same BF16 baseline/seeds now that `ModelFlux.keys_shape_critical` covers `txt_in.weight`/`vector_in.in_layer.weight` and `convert_safetensors.py` sets `full_precision_matrix_mult` for `nvfp4` layers (both fixed earlier the same day). 2 same-seed/prompt comparisons showed only minor palette/decorative variance (an earring color, a hat ornament) — composition, identity, pose, and outfit all preserved, the same bar FP8/INT8 already cleared. `safetensors_quant._RENDER_VERIFIED_MIXED` gains `("flux", "NVFP4")`/`("flux", "NVFP4_MIXED")`; `model_support.support_level()`'s NVFP4 branch now checks `_RENDER_VERIFIED_MIXED` at all (it never did before — an oversight from when that branch was written before flux had any verified NVFP4 evidence); `_RENDER_TESTED_DRIFT` (added earlier today for exactly this case) is empty again, though the CAUTION/UNKNOWN split it introduced stays. `lumina2`'s NVFP4/NVFP4_MIXED remain `_RENDER_CONFIRMED_BAD` — the same fixes apply to its converted output too (Z-Image Base was reconverted with `full_precision_nvfp4` active), but nobody has render-tested it since, so its verdict doesn't change on the strength of the flux result alone.

### Change
- **`⚠ Caution` split into `⚠ Caution` (tested, shows drift) and `? Unknown` (never tested)**: the Model Support tables previously used CAUTION for two different things — "never render-tested, no evidence either way" and, after `flux`'s NVFP4/NVFP4_MIXED render test showed 2-of-3-seeds composition drift, also "tested and it does something different from the baseline". A user needs to react to those differently (go test it, vs. judge whether the tradeoff is acceptable), so they now get separate states — mirroring the CAUTION/BAD split this table already made for the same reason. `model_support.py` adds `_RENDER_TESTED_DRIFT`/`_TE_RENDER_TESTED_DRIFT` sets (seeded with `("flux", "NVFP4")`/`("flux", "NVFP4_MIXED")`, the only current members); `support_level()`/`text_encoder_support_level()` now default to `SUPPORT_UNKNOWN` instead of `SUPPORT_CAUTION` for untested combinations, checking the new drift set first. `gui.py`'s format-dropdown annotation (`_annotate_choices_for_arch()`) now prefixes untested entries with `?` instead of `⚠`. The Model Support tab's legend and README wording updated to match — CAUTION's description changed from "technically supported, not render-tested" to "render-tested and shows some visible-but-tolerable difference from the uncompressed version".

### Added
- **Vendored config/tokenizer files for the documented candidate text-encoder base repos**: `text_encoder_configs/<name>/` now ships `config.json` + tokenizer files for CLIP-L, OpenCLIP-bigG, T5-XXL, Qwen3-4B, Qwen3-8B, Qwen2.5-VL-7B, Mistral-Small-3.2-24B, and the ERNIE-Image Prompt-Enhancer (Ministral3, from `baidu/ERNIE-Image`'s `pe/` subfolder). `fetch_base_config_files()` in `text_encoder_convert.py` now checks `_VENDORED_REPOS` (keyed by exact repo ID) first and copies from the vendored folder with no network call; unlisted repo IDs still fall back to the existing live HuggingFace download. Avoids a re-download on every conversion and keeps the tool working if a base repo is later gated/rate-limited/pulled.
- **Auto-detect the base model family from a text encoder's own weights, no manual repo ID needed**: `detect_text_encoder_family()` in `text_encoder_convert.py` reads `(hidden_size, num_hidden_layers, vocab_size)` straight off the checkpoint's embedding tensor + layer count and matches it against `_FAMILY_SIGNATURES` — verified unique across all 8 vendored families, including disambiguating Ministral3 (ERNIE-Image's prompt-enhancer) from Mistral-Small-3.2-24B, which share near-identical Llama-style key names and can't be told apart by key-name matching alone (unlike `models/architectures.py`'s `detect_arch()` for diffusion models). `convert_text_encoder()` now uses this when `base_repo_id` is blank, mirroring the diffusion-model conversion tabs where the source file alone is enough. The GUI's "Base model HF repo ID" field is now optional for GGUF output, documented as an override for base models outside the vendored candidate families.
- **Text Encoder Support table is now per-family, not one generic row**: `model_support.py` adds `TEXT_ENCODER_FAMILY_DISPLAY_NAMES`, `text_encoder_support_level(family, format_key)`, and `build_text_encoder_support_table()`, mirroring the diffusion-model table's per-architecture design now that `detect_text_encoder_family()` makes per-family identification possible. `gui._text_encoder_support_rows_for_dataframe()` renders one row per vendored family instead of one generic row. `qwen3-4b` is `SUPPORT_VERIFIED` on every format (GGUF direct/K-quant + all 4 safetensors formats convert+load+render-tested in ComfyUI, 4 seeds each, no format-specific defect found); every other vendored family stays `SUPPORT_CAUTION` (untested). Both support tables now share a literal red/yellow/green color legend (`--s2g-support-good/-caution/-bad` CSS custom properties) instead of reusing the app's teal `--s2g-accent` branding color for "verified".
- **`flux` architecture render-verified for FP8/FP8_MIXED/INT8/INT8_MIXED**: FLUX.2 Klein 9B (vanilla `black-forest-labs/FLUX.2-klein-base-9B` + its own Qwen3-8B text encoder), 2026-08-12: 3 same-seed/prompt comparisons (5 seeds total incl. batch variants) against the unquantized BF16 baseline showed all four formats producing output with zero visible deviation from baseline — identity, pose, outfit, background all matched exactly on every seed, a stronger result than the bar `lumina2`'s existing verified entries were held to. `safetensors_quant._RENDER_VERIFIED_MIXED` (name predates this addition — it now also covers non-`_MIXED` keys) gains `("flux", "FP8")`, `("flux", "FP8_MIXED")`, `("flux", "INT8")`, `("flux", "INT8_MIXED")`; `model_support.py`'s per-architecture table now shows `flux` as `✓ VERIFIED` for those four columns. `format_recommendation()` in `safetensors_quant.py` also stopped recommending against plain FP8/INT8 for architectures whose plain output is itself now render-verified — the old wording ("hasn't been render-tested on this specific architecture") would otherwise have become a false claim for `flux` specifically. NVFP4/NVFP4_MIXED were NOT promoted: the same seed batch showed visible composition drift (background details, once a missing garment) in 2 of 3 seeds for both variants — confirming, not just predicting by analogy, the CAUTION rating NVFP4 already carries architecture-wide (no format produced garbage/broken output, so nothing moved to BAD either).
- **`qwen3-8b` text-encoder family render-verified for FP8/FP8_MIXED/INT8/INT8_MIXED/NVFP4/NVFP4_MIXED**: FLUX.2 Klein 9B's own text encoder, 2026-08-13: 3 same-seed/prompt comparisons against the unquantized BF16 baseline for FP8/FP8_MIXED/NVFP4/NVFP4_MIXED, 2 for INT8/INT8_MIXED, showed zero visible deviation on every seed for every format — including NVFP4/NVFP4_MIXED, which *did* show visible composition drift when quantizing the FLUX.2 Klein DiT itself in the same session (see `flux`'s entry above). Text-encoder quantization only perturbs the conditioning vector fed into an otherwise full-precision DiT, not the sampling trajectory's own numerics, evidently a lot more forgiving than quantizing DiT weights directly. `model_support._TE_RENDER_VERIFIED` gains the six `("qwen3-8b", <format>)` pairs.
- **`qwen3-8b` GGUF (Q5_K_M) render-verified**: same FLUX.2 Klein 9B text encoder, 2 same-seed/prompt comparisons against the BF16 baseline — minor conditioning drift (a face-tattoo detail, a small ornament gap) but identical subject and composition on both seeds, comparable to the conditioning noise `qwen3-4b`'s GGUF K-quants already showed rather than a format-specific defect. `model_support._TE_RENDER_VERIFIED` gains `("qwen3-8b", "GGUF")`, completing every column for `qwen3-8b`.
- **INT8/INT8_MIXED added to the Text-Encoder Conversion format dropdown**: `TEXT_ENCODER_FORMAT_CHOICES`/`TEXT_ENCODER_SAFETENSORS_FORMATS` in `text_encoder_convert.py` gain `INT8`/`INT8_MIXED` alongside the existing `FP8`/`FP8_MIXED`/`NVFP4`/`NVFP4_MIXED` — `convert_text_encoder_to_safetensors()` already delegated to the same `quantize_tensor_st()` machinery that implements `int8_tensorwise`/ConvRot for diffusion models (`convert_safetensors.py`'s `_TARGET_TO_QUANT_FORMAT` already mapped these keys), the dropdown simply never listed them. `model_support.TEXT_ENCODER_TABLE_FORMATS` gains matching `INT8`/`INT8_MIXED` columns on the Text Encoder Support table, currently `⚠ Caution` for every family (not yet render-tested — this is a dropdown addition, not a support-level upgrade).
- **Estimated output size now shown on the Safetensors and Text-Encoder tabs, not just GGUF**: `safetensors_quant.py` adds `estimate_safetensors_output_size()`, a header-only per-tensor estimator that replicates `quantize_tensor_st()`'s exact branching (the `is_hiprec_st` 1D/small/`keys_hiprec` gate, `keys_shape_critical` fallback, NVFP4's block-scale swizzle padding, INT8 ConvRot's per-row scale) rather than GGUF's simpler unconditional-1D-F32 rule — needed because the `*_MIXED` formats' size depends on `model_arch.keys_hiprec`, which the existing GGUF estimator has no equivalent per-format dependency on. Cross-checked byte-for-byte against real `quantize_tensor_st()` output in `tests/test_safetensors_quant.py`. `gui.py` wires this into the Safetensors tab (architecture-detected, same as the existing dropdown ⚠/✗ annotations) and the Text-Encoder tab (using the fixed `_TEXT_ENCODER_MODEL_ARCH`); the Text-Encoder tab's GGUF-family formats (F32/F16/BF16/Q8_0/K-quants) use `quantize.py`'s plain `SIZE_RATIOS` ratio instead of its per-tensor analysis, since that analysis's F32-forcing heuristic was tuned for this project's own diffusion-model GGUF writer, not llama.cpp's `convert_hf_to_gguf.py`/`llama-quantize` (what text-encoder GGUF output actually goes through) — `SIZE_RATIOS`' own reference data (Llama-3-8B via `llama-quantize`) is the better-matched source for an LLM-style text encoder anyway.

### Added
- **`full_precision_nvfp4` — a candidate fix for NVFP4/NVFP4_MIXED's DiT corruption, NOT yet render-tested**: `convert_to_safetensors()` gains a `full_precision_nvfp4: bool = True` parameter mirroring the existing `full_precision_fp8`. Reading ComfyUI's actual `comfy/ops.py` source (a local ComfyUI-Easy-Install checkout, `comfy_kitchen` 0.2.30) found that `full_precision_matrix_mult` is read generically from any layer's `.comfy_quant` config in `_load_quantized_module()`, and `MixedPrecisionOps.Linear.forward()`'s `not self._full_precision_mm` gate that gets set from it is not FP8-specific — it applies identically to every `quant_format`, `nvfp4` included. `docs/issues_analysis.md` #16's "NVFP4 has no equivalent safe mode" conclusion was correct only for ComfyUI's own legacy-checkpoint upgrade path (`convert_old_quants()`, which really does only synthesize this flag for `scaled_fp8` checkpoints) — it never applied to this tool's own writer, which was simply never setting the flag for `nvfp4`. `convert_safetensors.py` now sets `"full_precision_matrix_mult": true` for `nvfp4` layers by default, same as it already does for `float8_e4m3fn`. Two new tests (`TestNvfp4FullPrecisionFlag`) mirror the existing FP8 flag tests. This is a source-reading-verified mechanism, not a render-tested fix — `lumina2`'s NVFP4/NVFP4_MIXED stay `_RENDER_CONFIRMED_BAD` and `flux`'s stay `⚠ Caution` in `model_support.py` until someone actually converts + loads + renders with this flag active. NVFP4/NVFP4_MIXED also stay out of the Safetensors tab's GUI dropdown until then, matching this project's standing evidence bar.

### Fix
- **`flux` plain NVFP4 crashed ComfyUI outright: `ModelFlux.keys_shape_critical` was missing `txt_in.weight` and `vector_in.in_layer.weight`**: found while test-driving the `full_precision_nvfp4` flag above — converting the vanilla FLUX.2 Klein 9B checkpoint to plain NVFP4 and loading it in ComfyUI crashed with `RuntimeError: mat1 and mat2 shapes cannot be multiplied (1024x12288 and 6144x4096)` inside `comfy_kitchen`'s NVFP4 dequantize fallback, at `txt_in`'s forward call. Root cause: `comfy/model_detection.py` reads `txt_in.weight.shape[1]` (raw, pre-dequant) to infer Flux's `context_in_dim`, and `vector_in.in_layer.weight.shape[1]` for `vec_in_dim` — the same class of bug `docs/issues_analysis.md` #9 already documented for `img_in.weight` (which was already protected). NVFP4 packs 2 values/byte, halving the on-disk last dim, so the Linear layer got built with half the correct input width. Both tensors were already in `keys_hiprec` (so `_MIXED` mode was never affected), just missing from `keys_shape_critical`, the list that also gates plain (non-`_MIXED`) NVFP4/INT8 shape safety. `models/architectures.py`'s `ModelFlux.keys_shape_critical` gains both keys; `tests/test_safetensors_quant.py`'s `test_nvfp4_shape_critical_protection_per_architecture` gains 2 regression cases. This is orthogonal to the `full_precision_nvfp4` flag itself — it's a pre-existing bug the flag's test conversion exposed (plain NVFP4's own dynamic-activation-quantization path silently tolerated the corrupted `context_in_dim` before; forcing full-precision compute made the resulting shape mismatch a hard crash instead of silent wrong output). Flux + Z-Image Base need reconverting and re-rendering against this fix before any `model_support.py` verdict changes.

### Fix
- **`qwen3-4b` INT8/INT8_MIXED render-verified clean — but only through ComfyUI's native `CLIPLoader` node**: an initial same-day batch of 8 renders across 6 different Z-Image checkpoints all produced full-image structured-noise garbage, briefly logged here as a confirmed-broken conversion. Root cause turned out to be a workflow error, not a conversion defect: every broken render loaded the `.safetensors` INT8 output through ComfyUI-GGUF's `CLIPLoaderGGUF` node (for `.gguf` files — the node most Z-Image workflows already default to for the text-encoder slot), which has no ConvRot/INT8-safetensors decode path and silently misreads the file as raw tensor data. The 3 renders that used the correct `CLIPLoader` node (Z-Image Base + its juggernautZ fine-tune, both INT8 and INT8_MIXED) were clean, no format-specific defect — found by comparing each render's embedded ComfyUI workflow metadata (`CLIPLoader` vs. `CLIPLoaderGGUF`) between the clean and broken sets. `model_support._TE_RENDER_VERIFIED` gains `("qwen3-4b", "INT8")` and `("qwen3-4b", "INT8_MIXED")`; `_TE_RENDER_CONFIRMED_BAD` reverts to empty for text encoders (no format has confirmed-broken *conversion* output). README now calls out the `CLIPLoader`-not-`CLIPLoaderGGUF` requirement explicitly.

### Fix
- **Safetensors/text-encoder conversion crashed mid-run on a Windows console stuck on a non-UTF-8 codepage (e.g. cp1252)**: `convert_to_safetensors()` and `convert_text_encoder_to_safetensors()`/`fetch_llama_cpp_convert_script()`'s `_log()` helpers fall back to plain `print(msg)` when no `on_log` callback is supplied (the GUI always supplies one, so this only bit CLI/script usage) — but the log text contains non-ASCII characters (e.g. `"->"` as U+2192), and `print()` raises `UnicodeEncodeError` on a console whose encoding can't represent them. The crash happened *before* `save_file()`, so the output file was never written, silently. `_log()`'s print fallback in `convert_safetensors.py` and both instances in `text_encoder_convert.py` now catch `UnicodeEncodeError` and retry with the console's own encoding in `errors="replace"` mode instead of aborting. Found while batch-converting a checkpoint via a standalone script for support-table render testing (no GUI, no `on_log`).
- **`_OPTIONAL_TOKENIZER_FILES` didn't cover Mistral's `tekken.json` or T5's `spiece.model`**: both filenames were missing from the tokenizer-file list `fetch_base_config_files()` tries, so Mistral- and T5-family base repos silently got no tokenizer file at all (only `config.json`). Found while vendoring config files for those two families and confirming what each repo actually ships. Added both filenames to `_OPTIONAL_TOKENIZER_FILES`.

### Corrected
- **Retracted the "text-encoder quantization shifts fine prompt semantics" claim from an earlier session (2026-08-12)**: that entry was based on a 2-seed same-prompt comparison where F16/Q8_0 looked clean and FP8/FP8_MIXED/NVFP4/NVFP4_MIXED/Q6_K all showed an unintended extra figure. A follow-up 4-seed batch showed the *unquantized F16 baseline itself* producing the same unintended figure on one of its four seeds, at the same ~1-in-4 rate as the quantized formats — so the effect isn't a quantization artifact at all, just prompt-adherence variance in the base model across seeds. No code or quantization behavior changed; only the (incorrect) causal claim is removed from README.md.

- **Text-encoder NVFP4 output NVFP4-packed the token-embedding table, breaking ComfyUI's plain (non-dequant) load path**: `text_encoder_convert.py` used a bare `ModelTemplate()` (empty `keys_shape_critical`) for the safetensors quant path, so `quantize_tensor_st()`'s existing NVFP4 shape-safety guard never applied to text encoders. NVFP4 halves a tensor's on-disk last dimension (2 values/byte) — safe for weights ComfyUI dequantizes through its `MixedPrecisionOps` Linear wrapper, but `nn.Embedding` tables are loaded via a plain `load_state_dict` with no such unpacking. Confirmed against a live ComfyUI install: Qwen3-4B's NVFP4 output raised `size mismatch for model.embed_tokens.weight: ... torch.Size([151936, 1280]) ... the shape in current model is torch.Size([151936, 2560])` (1280 == 2560 // 2). Added `_TEXT_ENCODER_MODEL_ARCH`, a `ModelTemplate()` instance with `keys_shape_critical = ["embed_tokens", "shared", "token_embedding", "wte", "lm_head"]` covering the common tied/untied embedding-table key names across this project's documented text-encoder families (Qwen3/Mistral, T5/UMT5, CLIP) — the same protection this pattern already gets for diffusion-model architectures. Verified against the real Qwen3-4B checkpoint: `model.embed_tokens.weight` now stays F16 at its original shape, no `.weight_scale` sidecar.

- **Text-encoder FP8/NVFP4/INT8 safetensors output stripped its own "model." prefix, breaking ComfyUI's text-encoder architecture detection**: `load_state_dict()`'s prefix-stripping rule (`_build_key_map()`) assumes a leading "model." wraps a diffusion UNet inside a larger checkpoint (e.g. a full checkpoint's `model.diffusion_model.*`) and strips it unconditionally — but a standalone HF Transformers text-encoder state dict genuinely uses "model." as its own real module path (e.g. Qwen3's `model.layers.0.self_attn.q_proj.weight`), and `convert_text_encoder_to_safetensors()` reused the same `load_state_dict()`/`convert_to_safetensors()` pipeline diffusion models use. Stripping the prefix produced an output file with keys like `layers.0.self_attn.q_proj.weight` instead of `model.layers.0...` — ComfyUI's own text-encoder architecture sniffing (`comfy/sd.py`'s `detect_te_model()`) checks for the literal prefixed key names and silently falls back to the wrong text-encoder class (a generic CLIP-L wrapper) when they're missing, producing `mat1 and mat2 shapes cannot be multiplied` or `Cannot copy out of meta tensor; no data!` at inference time — found via a live ComfyUI test with a real Qwen3-4B checkpoint, and confirmed against ComfyUI's actual `detect_te_model()` source (both probe keys present after the fix). `load_state_dict()`/`strip_prefix()`/`convert_to_safetensors()` gained a `strip_prefixes` parameter (default `True`, unchanged for diffusion models); `convert_text_encoder_to_safetensors()` now passes `strip_prefixes=False`.

- **Text-encoder FP8/NVFP4 safetensors conversion logged "INFO: Architecture: invalid"**: `convert_text_encoder_to_safetensors()` passes a bare `ModelTemplate()` into `convert_to_safetensors()` (text encoders aren't architecture-detected — see the Text-Encoder Conversion Pipeline docs), and `ModelTemplate.arch` defaults to the literal string `"invalid"`. `convert_safetensors.py` logged that value unconditionally, producing a confusing "architecture invalid" message even though the conversion completes correctly — `arch == "invalid"` is a deliberate sentinel, not a failure. The log line is now skipped for that sentinel, and the output file's `comfy.gguf_source_arch` metadata (same source, same misleading value) is omitted rather than written as `"invalid"`.

### Add
- **Text Encoder Support table** on the Model Support tab: a second, compact `gr.Dataframe` below the per-architecture table, covering the formats `Convert Text Encoder` offers (GGUF collapsed from F32/F16/BF16/Q8_0 + K-quants, plus FP8/FP8_MIXED/NVFP4/NVFP4_MIXED). Text encoders aren't in `models.architectures.arch_list` and have no `keys_hiprec`-style per-architecture risk model, so this is one generic row rather than per-architecture columns (`model_support.TEXT_ENCODER_TABLE_FORMATS`/`TEXT_ENCODER_SUPPORT`). Every format is currently `⚠ Caution`: none has a documented ComfyUI load+render confirmation by this project. Clicking a cell jumps to the Convert Text Encoder tab with that format pre-selected (`gui.apply_text_encoder_support_table_selection()`) — unlike the diffusion-model table, NVFP4 isn't excluded here since it's a real `TEXT_ENCODER_FORMAT_CHOICES` entry.

### Change
- **Model Support table split its "Caution" symbol into two states**: `⚠ Caution` (technically supported, not render-tested for this architecture — no evidence either way) previously also covered actually-confirmed-wrong combinations, making the table unable to distinguish "never tried" from "tried and broke". Added `SUPPORT_BAD`/`✗ Known issue` for combinations with direct render-test evidence of wrong output (`model_support._RENDER_CONFIRMED_BAD`): plain `INT8` on `lumina2`, and `NVFP4`/`NVFP4_MIXED` on `lumina2` (docs/issues_analysis.md #15). The format-dropdown ⚠ annotation (`gui.py`'s `_annotate_choices_for_arch()`) now prefixes ✗ for these same combinations instead of silently losing its warning marker.

### Fix
- **`lumina2` + FP8_MIXED is now `✓ Verified`, not `⚠ Caution`**: a second same-seed/prompt user comparison of Z-Image Base output (uncompressed vs. this tool's `FP8_MIXED`) showed composition, pose, face, makeup, jewelry, hair, and outfit all preserved — the only deviation was the color/shape of a single secondary prop, judged tolerable render-to-render quantization variance rather than a correctness failure, the same bar INT8_MIXED was already held to. `safetensors_quant._RENDER_VERIFIED_ARCHES` (a flat set of arch keys, implicitly INT8_MIXED-only) was generalized to `_RENDER_VERIFIED_MIXED` (`(arch_key, format_key)` pairs, mirroring `_RENDER_CONFIRMED_BAD`'s shape) and now includes both `("lumina2", "INT8_MIXED")` and `("lumina2", "FP8_MIXED")`. `format_recommendation()` no longer discloses the "not yet confirmed by a render test" caveat for `lumina2` + FP8_MIXED.

- **Plain FP8 on `lumina2` is now marked `✗ Known issue`, not `⚠ Caution`**: a same-seed/prompt comparison of Z-Image Base output (uncompressed vs. this tool's plain `FP8`) showed the standing figure's outfit and the entire room background changed between the two renders, with only the figures' poses staying recognizable — the same "wrong identity/composition, pose skeleton survives" corruption pattern `docs/issues_analysis.md` #15 documented for pre-fix FP8_MIXED and plain INT8. `model_support._RENDER_CONFIRMED_BAD` and `safetensors_quant._RENDER_CONFIRMED_BAD_PLAIN` now include `("lumina2", "FP8")`; `format_recommendation()`'s warn message states the corruption as confirmed rather than merely predicted by analogy from INT8's confirmed case. FP8_MIXED's status is unaffected — only plain FP8 was exercised by this comparison.

- **The 7th tab ("Model Support") got hidden behind Gradio's "..." tab overflow menu** on real-world renders (Windows/Brave font-fallback rendering measures noticeably wider than in-CI Chromium) — `.gradio-container` widened from 1100px to 1600px and the double-space after each tab's leading icon trimmed to single-space, giving the tab-nav enough room that Gradio's own width measurement keeps every tab on one row instead of auto-collapsing the last two.

- **FP8 wrongly inherited INT8's lumina2 render-verified status**: the previous review fix's shared FP8/INT8 branch in `format_recommendation()` reused `_RENDER_VERIFIED_ARCHES` unconditionally, so `lumina2` + FP8/FP8_MIXED silently inherited INT8_MIXED's actual render-test evidence — plain FP8 claimed "has shown visible pose/identity corruption in testing" (an INT8-only finding) and FP8_MIXED dropped its unverified caveat, both contradicting `model_support.py`'s FP8/FP8_MIXED=`SUPPORT_CAUTION` (no FP8 output from this tool has ever been render-tested). `verified` is now INT8-only. Found by an independent codex-mcp review pass.

- **Final whole-branch review findings for the Model Support tab** (see `docs/superpowers/plans/2026-08-10-model-support-tab.md`):
  - `gui.py` had a mid-file `from model_support import ...` violating the project's `ruff` E402 CI gate — moved to the top-level imports.
  - The Model Support table's NVFP4/NVFP4_MIXED cells were clickable and, via `apply_support_table_selection()`, set the Safetensors format dropdown to a value outside its own `SAFETENSORS_DTYPE_CHOICES` — silently letting a user reach NVFP4 output through the GUI despite it being deliberately unoffered (no verified safe mode, `docs/issues_analysis.md` #16). Those cells are now a no-op, like the Model-name column.
  - `format_recommendation()`'s FP8 branch returned an unconditional "ok" for every architecture, but `quantize_tensor_st()` only skips keys_hiprec-sensitive tensors when `mixed=True` — identically for FP8 and INT8 — so plain FP8 carried the same sensitive-architecture precision risk plain INT8 already warns about. FP8 now shares INT8's sensitivity-aware warn/ok logic.
  - `model_support.support_level()` marked FP8/FP8_MIXED `SUPPORT_VERIFIED` unconditionally, but that status is defined as an actual convert+load+render confirmation in ComfyUI — FP8's safety fix rests on a code-reading investigation only, no render test has confirmed it on any architecture. Downgraded to `SUPPORT_CAUTION` (same bar as NVFP4) until that test happens.
  - `build_support_table()` did a bare `MODEL_DISPLAY_NAMES[...]` index that would crash the entire GUI at startup if a future architecture were added without a matching display-name entry; now falls back to the raw arch key via `.get()`.
  - `quantize.py`'s `ALL_QUANT_CHOICES` comment claimed its size-savings percentages were "derived from `SIZE_RATIOS`", but they're hand-written literals with nothing enforcing the two stay in sync — comment corrected, and a new test (`tests/test_quantize.py::test_size_savings_percentages_match_size_ratios`) now checks the numbers against `SIZE_RATIOS` directly.
  - `_RENDER_VERIFIED_ARCHES` was defined independently in both `safetensors_quant.py` and `model_support.py`, "mirrored" by comment only — `model_support.py` now imports the one in `safetensors_quant.py` instead of keeping its own copy.
  - `st_src_path.change()` fired two separate handlers (`update_format_recommendation`, `annotate_safetensors_choices`) that each independently re-detected the source architecture (a full `torch.load()` for non-safetensors sources); merged into one `update_format_recommendation_and_choices()` handler that detects once.
  - `annotate_gguf_choices()` paid the same full detection cost on every GGUF-tab source-path change for a result that's mathematically guaranteed to never differ from the unmodified choices (`support_level()` has no per-architecture GGUF branch) — now skips detection entirely.

- **Convert → Safetensors intro text was stale**: it still said "FP8/NVFP4/MXFP8 are not offered here", left over from before FP8/FP8_MIXED were re-enabled in the dropdown. Now explains FP8's `full_precision_matrix_mult` safety flag and clarifies that only NVFP4/MXFP8 remain unoffered.

- **FP8 safetensors output took ComfyUI's risky dynamic-activation-quantization path, unlike the scaled-FP8 checkpoints already circulating on Civitai/HuggingFace** — `comfy/utils.py`'s `convert_old_quants()` (how ComfyUI loads those legacy community checkpoints) sets `"full_precision_matrix_mult": true` for them, which makes `comfy/ops.py`'s `MixedPrecisionOps.Linear.forward()` skip the quantized-compute branch and run a plain full-precision matmul instead; this tool's own `convert_safetensors.py` never set that flag. `convert_to_safetensors()` gained a `full_precision_fp8: bool = True` parameter that now writes `"full_precision_matrix_mult": true` into every FP8/FP8_MIXED layer's `.comfy_quant` config by default, matching that same safety profile architecture-independently (unlike INT8's per-architecture `keys_hiprec` bet). See `docs/issues_analysis.md` #16. NVFP4's equivalent investigation (whether it has any comparable safe-mode flag) is explicitly **not done** — open follow-up, `model_support.py`'s `support_level()` docstring keeps NVFP4/NVFP4_MIXED at `SUPPORT_CAUTION` everywhere pending it.

- **`format_recommendation()`'s plain-INT8 warning overclaimed for unverified architectures**: it asserted "plain INT8 has shown visible pose/identity corruption in testing" uniformly for every architecture with a non-empty `keys_hiprec`, but only `lumina2` has actually been render-tested this way. Now mirrors the `ok` branch's existing honesty split: `lumina2` keeps the strong claim, every other sensitive architecture gets a claim scoped to what's actually known (the same class of risk caused corruption on `lumina2`, not confirmed by a render test on this architecture).

### Change
- **Header mark replaced**: the CSS-only hex-lattice pattern in the app header is now a custom icon (`assets/header_logo.png`, files → quantized-block motif) instead. Embedded as a base64 `data:` URI at import time (`gui.py`'s `_load_header_logo_data_uri()`) so it stays self-contained — no external request, no Gradio static-file route to configure — consistent with the app's no-outbound-requests-at-startup policy. Source PNG cropped/downscaled from a 1254×1254 original to a 240×234 tile to keep the embedded size small (~55 KB).

### Add
- **FP8/FP8_MIXED re-enabled in Convert → Safetensors**, now that `convert_safetensors.py` defaults FP8 output to `full_precision_matrix_mult=true` (previous entry) — re-added to `SAFETENSORS_DTYPE_CHOICES`, and `safetensors_quant.format_recommendation()` returns an unconditional "ok" for FP8, unlike INT8's per-architecture caution.
- **GGUF quant dropdown labels gained size-savings percentages** ("N% smaller than F16"), derived from the existing `SIZE_RATIOS` table so the two can't drift apart. Quality wording (already present) is unchanged.
- **Dynamic ⚠ warning icons on the format dropdowns**: once a source checkpoint's architecture is detected, the Convert → Safetensors format dropdown (and, structurally, the Convert → GGUF quant dropdown, though it never fires today since every GGUF entry is currently `SUPPORT_VERIFIED`) prefixes any `SUPPORT_CAUTION` entry (per `model_support.support_level()`) with a ⚠. Entries stay fully selectable — this only adds information, it never blocks a choice. `gui.annotate_safetensors_choices()`/`gui.annotate_gguf_choices()`, wired as additional `.change()` handlers on `st_src_path`/`src_path` alongside the existing format-recommendation and size-estimate handlers.
- **Click-to-apply on the Model Support table**: clicking a format cell now switches to the matching Convert tab (GGUF -> `id=0`, Safetensors formats -> `id=1`, via `main_tabs`) and pre-selects that format in the tab's dropdown (`quant_dropdown`/`st_format_dropdown`). `gui.apply_support_table_selection()`, wired to `support_table.select(...)`. The GGUF column collapses every K-quant level into one cell, so it always pre-selects `Q4_K_M` (the tool's own "recommended ★" default); clicking the Model-name column is a no-op.
- **New "Model Support" GUI tab**: renders `model_support.build_support_table()` as a read-only `gr.Dataframe(datatype="html")` — one row per detectable architecture, one colored ✓/⚠/? cell per `TABLE_FORMATS` column (`gui._support_table_cell_html()`/`gui._support_table_rows_for_dataframe()`), reusing the existing `--s2g-accent`/`--s2g-warn`/`--s2g-muted` CSS tokens, plus a legend explaining the tri-state scheme. The other six tabs gained explicit `id=0..5` and the `gr.Tabs()` context is now captured as `main_tabs`, both needed by the next task's click-to-apply wiring (jump to a Convert tab with the clicked format pre-selected).
- **Per-architecture format support table in `model_support.py`**: `build_support_table()` returns one dict row per models.architectures.arch_list entry with architecture name, display name, and support level for each TABLE_FORMATS column — ready for the GUI's Model Support tab (Task 6) to render directly without re-deriving per-architecture sensitivity itself.
- **Per-architecture format recommendation in Convert → Safetensors**: mirrors the GGUF tab's static "recommended ★" label on `Q4_K_M`, but per-architecture — unlike GGUF K-quants (a uniform quality/size tradeoff), plain INT8 measurably corrupts output (wrong poses/identities) on attention-sensitive DiT architectures that need `keys_hiprec` protection, while costing nothing to recommend on architectures that don't need it. `safetensors_quant.format_recommendation(model_arch, target_key)` returns an ("ok"/"warn", message) pair; `gui.py`'s `update_format_recommendation()` detects the source checkpoint's architecture from its header only (no tensor data loaded) and shows a colored badge under the format dropdown, updated on both source-path and format changes. Discloses which architectures' protection lists are actually render-verified (currently only `lumina2`) versus matched against a community reference but not yet confirmed end-to-end, rather than implying uniform confidence. `INT8_MIXED`'s dropdown label also gained the same static "recommended ★" marker as its default-selected status already implied.
- **Automatic Dequantization**: `convert_to_safetensors()` no longer refuses outright when the source checkpoint is already quantized (e.g. a published ComfyUI-native `int8_tensorwise`/ConvRot or scaled-FP8 release) — it now detects each already-quantized `.weight` layer and reconstructs an approximate float32 original before feeding it through the normal quantization pipeline, so it can be cleanly re-quantized to a different target format instead of erroring out (mirrors Starnodes ComfyUI Model Converter's "Automatic Dequantization" feature). New `dequantize.py`: `detect_quantized_weight()` reads the authoritative per-layer `.comfy_quant` JSON sidecar first (falling back to a dtype + sibling-`.weight_scale` heuristic when absent/unparseable — real-world checkpoints don't always carry the sidecar), `dequantize_weight()` reverses `int8_tensorwise` (plain and ConvRot — un-rotates via the Hadamard matrix's orthogonality, `W = W_rot @ H`), scaled FP8, and NVFP4 (reusing the existing `dequantize_nvfp4()`). The old hard guard is narrowed to its one genuinely unrecoverable case: an int8/uint8 `.weight` with no recognizable scale sidecar at all (no scale means no way to reconstruct the original magnitude).

### Correction
- **The FP8_MIXED/NVFP4_MIXED root-cause citation below (Comfy-Org/ComfyUI#14595) was wrong** — read against the issue's own text, it documents a *performance-only* regression (some MLP GEMMs silently fall back to bf16 instead of FP8, ~15% below expected speedup) — mathematically correct output, just slower. It does **not** describe or explain the corruption (black bars, wrong poses/identities, full-image noise) this project observed and root-caused separately. The INT8-only decision itself is unaffected: it rests on the `QUANT_ALGOS["quantize_input"]` registry distinction (FP8/NVFP4/MXFP8 dynamically quantize activations, `int8_tensorwise`/`convrot_w4a4` don't), the `full_precision_matrix_mult: true` experiment (removed the black bar, implicating the quantized-compute path, not on-disk data), and independent third-party confirmation (`marcorez8/Z-image-aka-Base-nvfp4`'s own tiered quality ratings show real NVFP4 degradation on Z-Image at every protection level up to and including our own `keys_hiprec` scope). See `docs/issues_analysis.md` #15's Correction note for the full re-examination.

### Change
- **GUI visual redesign**: replaced Gradio's stock indigo/sky/slate theme hues and the isolated purple/blue gradient header with a deliberate design system — named CSS custom-property tokens (`--s2g-*`, light values on `:root`, dark values re-declared under Gradio's own `:root.dark, :root .dark` selector, not `@media (prefers-color-scheme)`, so it stays in sync with Gradio's actual dark-mode toggle including a manual `?__theme=` override) driving a cyan/teal accent palette, a Windows-native font stack (`Segoe UI Variable`/`Aptos` with system-font fallbacks — no CDN fetch, keeping the existing telemetry-off/no-outbound-requests startup policy), and an explicit type scale (`--type-display/body/small`). The header is now a "compiler console" banner: a CSS-only blueprint grid + a hex-lattice motif (`repeating-linear-gradient`, clipped to a hexagon) echoing the tab icons' ⬡ motif, instead of a plain gradient block. Tab labels get a distinguishing glyph each (⬡ ⬢ ✎ ⚒ ⚙ ▤); each tab's description text is now a set-off "intro" info box instead of bare Markdown; the settings card gets a `:focus-within` accent border so it's clear which section is active; buttons get a `scale(0.98)` `:active` press state. Verified in both light and dark mode via a live-launched instance. `models/architectures.py`/conversion logic untouched — CSS/labels only.

### Fix
- **GUI "Convert → Safetensors" tab's log box never showed which tensor was being processed**, unlike "Convert → GGUF" — `convert_to_safetensors()` only called `on_log` for the architecture/writing/done lines, never per-tensor (`convert.py`'s `handle_tensors()` already logged a throttled per-tensor line via `log_tensor_every`). Added the same `log_tensor_every` parameter/throttling to `convert_to_safetensors()` and wired `gui.py`'s existing `GUI_TENSOR_LOG_EVERY` constant through `run_st_convert()`, matching the GGUF tab's behavior.

### Add
- **Text-encoder conversion now supports the full GGUF K-quant palette and FP8/NVFP4 safetensors output**, not just F32/F16/BF16/Q8_0. `text_encoder_convert.py` gains `TEXT_ENCODER_FORMAT_CHOICES` (replaces the GUI-facing `TEXT_ENCODER_OUTTYPES` dropdown) covering three backends:
  - `Q6_K`/`Q5_K_M`/`Q4_K_M`/`Q4_K_S`/`Q3_K_M`/`Q2_K`: `convert_text_encoder_kquant()` runs the existing F16 GGUF conversion, then quantizes with `run_quantize()` (reused from `quantize.py`) using a **new, separate** plain llama-quantize binary — `ensure_plain_llama_quantize()` builds it once via `cmake` from the already-auto-cloned llama.cpp checkout (cached under `.llama.cpp/build-quantize/`). Deliberately distinct from the City96-patched binary used for diffusion-model GGUFs, which `docs/building-llama-quantize.md` documents as unsafe for LLM/text GGUFs.
  - `FP8`/`FP8_MIXED`/`NVFP4`/`NVFP4_MIXED`: `convert_text_encoder_to_safetensors()` reuses `convert_safetensors.convert_to_safetensors()` (which gained an optional `model_arch` parameter) with a bare `ModelTemplate()` instance instead of the diffusion-model architecture detection — text encoders aren't in `arch_list` and don't need to be: ComfyUI's text-encoder loaders build models from fixed config presets rather than inferring hyperparameters from checkpoint tensor shapes, so the generic 1D-skip/16-multiple-fallback safety rules are sufficient (no per-architecture `keys_shape_critical` audit needed, unlike the diffusion-model NVFP4 fix above). No HuggingFace download or llama.cpp involved for this path at all.
  - `convert_text_encoder_any()` is the new single dispatcher `gui.py`'s `run_te_convert()` calls, branching on the selected format.

### Fix
- **FP8_MIXED/NVFP4_MIXED diffusion-model output remained visibly wrong (black bars,
  mirrored composition, wrong poses/identities on FP8; full-image noise on NVFP4) even
  after fixing every on-disk data bug in this tool (see the four entries below and
  `docs/issues_analysis.md` #10-#14)** — root cause traced to ComfyUI itself, not the
  file this tool writes. A full weight audit proved the FP8_MIXED file's data 100%
  correct; forcing `full_precision_matrix_mult: true` removed the black bar but not the
  pose/identity drift; protecting `attention.qkv`/`attention.out` via
  `ModelLumina2.keys_hiprec` (confirmed to cover a large fraction of the model's
  parameters, by output file size) changed nothing either. Reading ComfyUI's own
  `comfy/quant_ops.py` `QUANT_ALGOS` registry showed `float8_e4m3fn`/`nvfp4` dynamically
  quantize *activations* at inference time (unlike GGUF, which always dequantizes
  weights to full precision before a standard matmul) through a path with a confirmed
  architecture-dependent bug (Comfy-Org/ComfyUI#14595, filed against a similarly
  new/custom DiT architecture). `int8_tensorwise`/`convrot_w4a4` are the only formats
  ComfyUI marks `"quantize_input": False` — weight-only quantization, sidestepping that
  runtime path entirely. Added `safetensors_quant_int8.py` implementing
  `int8_tensorwise` (single tensor-wise scale, or — where `in_features % 256 == 0` — an
  offline block-Hadamard rotation (ConvRot) plus per-row scale, both algorithms and the
  `.comfy_quant` metadata schema matched against `Comfy-Org/comfy-kitchen`'s and
  `Comfy-Org/ComfyUI`'s published source). Replaced `FP8`/`FP8_MIXED`/`NVFP4`/`NVFP4_MIXED`
  with `INT8`/`INT8_MIXED` in `SAFETENSORS_DTYPE_CHOICES` (the diffusion-model Convert →
  Safetensors GUI tab) — the FP8/NVFP4 implementations stay in the codebase (still
  correct for what they claim to do) but are no longer GUI-selectable given ComfyUI's
  known-buggy consumption path for this architecture family. Text-encoder FP8/NVFP4
  output (a separate registry, `text_encoder_convert.py`) is unaffected/unchanged. See
  `docs/issues_analysis.md` #15.
- **`*_MIXED` safetensors output could end up *larger* than the unquantized bf16 source**:
  `quantize_tensor_st()`'s mixed-hiprec branch force-upcast every protected tensor to
  `torch.float32` regardless of source dtype — harmless while `keys_hiprec` covered only a
  few small tensors, but once extended for Lumina2 (attention + `adaLN_modulation` +
  `final_layer`, see above) this doubled the on-disk size of a large fraction of the model's
  bf16-sourced weights for zero precision benefit (ComfyUI casts every loaded weight to its
  own `compute_dtype` regardless of on-disk dtype). Concretely: `INT8_MIXED` output for a
  12.31 GB source came out at 12.57 GB. Fixed by casting hiprec tensors to their original
  dtype (`data.to(old_dtype)`) instead of a forced F32 — same checkpoint now produces an
  8.30 GB file. Added `test_mixed_hiprec_tensor_keeps_original_dtype_not_forced_f32`. See
  `docs/issues_analysis.md` #15.
- **`ModelLumina2.keys_hiprec` was still narrower than the established community
  consensus for this exact model**: even after protecting attention/modulation/final_layer,
  end-to-end testing (same prompt/seed vs. the unconverted original) showed a
  checkpoint-independent identity/pose difference on both a fine-tune and the official
  `zImageBase_base` checkpoint. `tritant/ComfyUI_Kitchen_nvfp4_Converter`'s published
  `"Z-Image-Base"` blacklist additionally protects `cap_embedder`/`x_embedder`/`t_embedder`/
  `noise_refiner`/`context_refiner` — the submodules that build the initial noise/caption
  representation before the main transformer runs. Added all five (verified against real
  tensor names in a live checkpoint first) and broadened the attention match from
  `"attention.qkv"`/`"attention.out"` to plain `"attention"`. Output size grew from 8.30 GB
  to 8.77 GB (still ~29% smaller than the 12.31 GB source). End-to-end verification (same
  prompt/seed, both a fine-tune and the official checkpoint) confirmed pose/composition/
  identity now closely match the unconverted original. See `docs/issues_analysis.md` #15.
- **Extended `keys_hiprec` for every other architecture this project supports that
  tritant/ComfyUI_Kitchen_nvfp4_Converter also has a published per-architecture blacklist
  for**, since `ModelFlux` and `ModelQwenImage` had *no* `*_MIXED` protection at all and
  `ModelWan`/`CosmosPredict2` had only one tensor each protected — the same class of risk
  Lumina2 turned out to have, just not yet reported for these. `ModelFlux` gains
  `img_in`/`txt_in`/`time_in`/`vector_in`/`guidance_in`/`final_layer`/`class_embedding`/
  modulation tensors; `ModelQwenImage` gains `img_in`/`txt_in`/`time_text_embed`/
  `norm_out`/`proj_out`/`img_mod.1`; `ModelWan` adds `text_embedding`/`time_embedding`/
  `time_projection`/`head`; `CosmosPredict2` (the architecture Anima checkpoints use) adds
  `llm_adapter`/`final_layer`/`proj_out`/`x_embedder`/`t_embedder`/`context_embedder`
  (cross-checked against `Comfy-Org/comfy-quants`' own `anima.md`, which independently
  protects the same set). Not end-to-end image-tested for these four (no checkpoints
  available this session) — adopted on the strength of an established community tool's
  configuration, unlike the Lumina2 fix which was verified against real renders. See
  `docs/issues_analysis.md` #15.
- **Non-mixed NVFP4 crashed on Lumina2's pad-token parameters**: `ModelLumina2.keys_hiprec = ["x_pad_token", "cap_pad_token"]` only protects those tensors under `*_MIXED` targets (`quantize_tensor_st()` only consults `keys_hiprec` when `mixed` is `True`). Under plain `NVFP4`, they got packed like any other 2D tensor, halving their last dimension — and since ComfyUI's `NextDiT.__init__` hardcodes these `nn.Parameter`s to a fixed shape from the detected architecture (unlike `cap_embedder.1.weight`, which corrupts an *inferred* config), the mismatch is a hard crash (`size mismatch for x_pad_token: … [1, 1920] … [1, 3840]`) rather than silently-wrong values. Fixed by adding `"x_pad_token"`/`"cap_pad_token"` to `ModelLumina2.keys_shape_critical`, which — unlike `keys_hiprec` — is checked unconditionally. Added `test_nvfp4_pad_token_falls_back_to_f16_unconditionally`. See `docs/issues_analysis.md` #14.
- **NVFP4 output was nibble-swapped for the real ComfyUI inference kernel, even with every value/scale/layout check passing**: `comfy_kitchen`'s standalone `dequantize_nvfp4()` helper exposes a `hi_first` nibble-order parameter, but the function ComfyUI's real inference path actually calls — `scaled_mm_nvfp4()`, the fused GEMM kernel — has **no such parameter**; its convention is hardcoded to `hi_first=True` (even-indexed element in the high nibble). `safetensors_quant_nvfp4.py`'s `quantize_nvfp4()` packed the opposite way. This was invisible to every dequantize-based check in this investigation — including auditing all 413 weight tensors in a real converted file against the original checkpoint, all found byte/value-correct — because those checks all went through `dequantize_nvfp4()` (either this tool's own, self-consistent by construction, or `comfy_kitchen`'s with `hi_first` set to match). Found only by noticing `ck.scaled_mm_nvfp4`'s signature has no `hi_first` argument at all. Fixed by swapping the packing order; verified directly against `scaled_mm_nvfp4` (not `dequantize_nvfp4`): a weight quantized by this tool now matches a full `comfy_kitchen`-native ground truth matmul to within normal 4-bit×4-bit quantization noise (13.22% vs. 13.01% relative mean error). Added `test_packs_even_index_in_high_nibble_hi_first_true`, which checks a hand-computed packed byte value rather than a round trip. See `docs/issues_analysis.md` #13.
- **Re-quantizing an already-quantized source checkpoint silently corrupted it**: some published checkpoints (e.g. ComfyUI-native `int8_tensorwise`+ConvRot releases) are already partially quantized, storing raw integer weights alongside their own pre-existing `.weight_scale`/`.comfy_quant` sidecar tensors right in the state dict. This tool had no concept of that — it iterated every tensor and re-quantized each one as an ordinary weight, including the pre-existing scale/metadata sidecars themselves (a `[rows,1]` float32 `.weight_scale` tensor looks just like a small 2D weight matrix), producing corrupted output with mis-named `_quantization_metadata` layer keys (an extra `.weight_scale` suffix, since `layer_key()` only strips a trailing `.weight`). Found by tracing a user's FP8_MIXED noise-image report: 170 of 378 layers in the output had corrupted metadata keys, all tracing back to a source checkpoint (`juggernautZ_v10ByRundiffusion.safetensors`) that turned out to already be `int8_tensorwise`-quantized — confirmed by its own embedded `_quantization_metadata`. Fixed by adding `_check_not_already_quantized()` (`convert_safetensors.py`), which raises before conversion starts if any `.comfy_quant` sidecar key is present anywhere in the state dict, or any `.weight` tensor is stored as int8/uint8 (bare float8 weights are still allowed through — an existing, deliberately-supported case, coerced to float16). Verified against both the corrupted source (raises) and a genuinely unquantized sibling file in the same directory (does not raise). See `docs/issues_analysis.md` #12.
- **Every NVFP4 safetensors conversion decoded every weight at exactly half its intended magnitude**: `safetensors_quant_nvfp4.py`'s `_KVALUES` E2M1 decode table used `gguf.quants.NVFP4.kvalues`'s **doubled** magnitudes (`0,1,2,3,4,6,8,12,…`), per its own docstring claim of matching that table. But gguf's table is doubled specifically to compensate for a *different*, custom per-block scale encoding gguf's NVFP4 uses internally (`ue4m3_to_fp32`, which bakes in a `*0.5` decode factor) — this tool never used that encoding, it writes plain `torch.float8_e4m3fn` block scales (what ComfyUI's `comfy_kitchen` actually expects), so pairing that with gguf's doubled table silently halved every decoded weight. Reported by a user as pure-noise/garbage NVFP4_MIXED output persisting even after the shape-crash and swizzle-layout fixes below. This tool's own round-trip tests never caught it — quantizing and dequantizing with the same (wrong) table cancels the error out, only surfacing against ComfyUI's real, independent decoder. Found by comparing `quantize_nvfp4()` output directly against `comfy_kitchen.tensor.nvfp4.TensorCoreNVFP4Layout`'s own quantize/dequantize on a live ComfyUI install: a block filled with the exact table value `6.0` decoded back to `3.0`, showing the 2x error directly. Fixed by replacing `_KVALUES` with the standard undoubled OCP E2M1 magnitudes (`0,0.5,1,1.5,2,3,4,6,…`) — the existing `/6.0` scale divisors already assumed this range. Verified against `comfy_kitchen`'s own reference on a random tensor: mean reconstruction error 0.2857 (ours) vs. 0.2856 (ComfyUI's own). Added `test_kvalues_are_standard_undoubled_e2m1_magnitudes` and `test_block_of_exact_table_values_round_trips_exactly`.
- **Every NVFP4 safetensors conversion produced a `.weight_scale` tensor in the wrong on-disk layout for every 2D (Linear) weight** — not just the one layer a user hit a crash on: `safetensors_quant_nvfp4.py` wrote each layer's `.weight_scale` as a plain, unpadded `(rows, k_blocks)` tensor, but ComfyUI's `comfy_kitchen` CUDA backend (`scaled_mm_nvfp4`) requires block scales padded to `(roundup(rows,128), roundup(k_blocks,4))` **and** rearranged into a 128x4 block-interleaved layout — the same convention vLLM's `swizzle_blockscale()` and TensorRT-Model-Optimizer NVFP4 checkpoints use. Concretely, this means two distinct symptoms depending on the layer's shape, both present in every prior NVFP4/NVFP4_MIXED conversion:
  - Layers whose output width isn't already a multiple of 128 (or block count a multiple of 4) fail the kernel's shape assertion outright — reported by a user as a crash (`AssertionError: Invalid B scale shape`) on Lumina2/Z-Image's `final_layer.linear.weight`.
  - Layers whose dimensions already happen to be multiples of 128/4 (most transformer block weights — hidden_size, mlp_dim, …) pass the shape check, but the scale values are read in the wrong order by the kernel, since a plain zero-padded tensor and the required interleaved layout have the same shape but different element order. This loads silently wrong (not crashing) weights.

  Root-caused by reading `comfy_kitchen`'s CUDA backend source directly (`roundup_n`/`roundup_sk` formula) and cross-verified against vLLM's open-source `nvfp4_utils.py` implementation of the identical layout. Fixed by adding `_swizzle_block_scale()`/`_unswizzle_block_scale()` (2D weights only — the case that reaches this kernel path) to `safetensors_quant_nvfp4.py`, applied unconditionally in `quantize_nvfp4()`. Existing NVFP4 tests updated for the new padded/swizzled `weight_scale` shape; added `test_scale_swizzled_shape_matches_comfy_kitchen_kernel_formula` and `test_swizzle_matches_vllm_reference_implementation` (a literal transcription of vLLM's function, since a self-inverting swizzle/unswizzle round-trip test alone can't detect a layout that's internally consistent but wrong relative to the real kernel).
- **`ensure_plain_llama_quantize()` couldn't actually use an auto-detected Visual Studio on Windows**: `_locate_msvc_build_env()` (added alongside the K-quant feature above, for the common case where VS is installed but its tools were never added to the user's PATH) had two bugs, both found and fixed via a real end-to-end build on a Windows machine with VS installed but nothing on PATH:
  - The `cmd /c "call \"<vcvarsall>\" x64 && set"` invocation used to capture vcvarsall's environment variables failed outright — `subprocess`'s Windows argument-quoting (`list2cmdline`) re-quotes an argument that itself contains both spaces and quotes (the vcvarsall path), mangling the nested quotes so cmd.exe couldn't find the batch file at all (silent `FileNotFoundError`, caught and treated as "no VS found"). Fixed by writing the `call`+`set` sequence to a temp `.bat` file and running that instead — sidesteps the nested-quoting problem entirely.
  - Even with the environment captured correctly, passing it as `subprocess.run(["cmake", ...], env=build_env)` still raised `FileNotFoundError`: on Windows, `env=` only sets the **child** process's environment block — the executable itself is still resolved against the **calling** process's own `PATH`, not `build_env`'s. Fixed by resolving `cmake`'s absolute path via `shutil.which("cmake", path=build_env["PATH"])` before invoking it, instead of relying on Windows to search `build_env["PATH"]` (it doesn't). Added `test_resolves_cmake_absolute_path_from_located_msvc_env` regression test.
- **FP8/NVFP4 safetensors output was silently broken in ComfyUI**: `safetensors_quant_fp8.py`/`safetensors_quant_nvfp4.py` named the per-layer scale sidecar tensor `<full_tensor_name>.weight_scale` (e.g. `x_embedder.weight.weight_scale`), but ComfyUI's quantized-op loader (`comfy/ops.py _load_quantized_weight_body`, confirmed by reading a live ComfyUI 0.29.2 install) looks up `<module_prefix>weight_scale` — a **sibling** of `<module_prefix>weight`, not nested under it (e.g. `x_embedder.weight_scale`). The mismatch meant every `.weight_scale`/`.weight_scale_2`/`.comfy_quant` tensor we wrote was reported by ComfyUI as an "unexpected key" and silently discarded: FP8 checkpoints loaded the raw unscaled float8 bytes as weights (visible as solid-noise output images), and NVFP4 additionally crashed on some checkpoints (see below). Root-caused via a user bug report showing garbage Z-Image output from a `FP8_MIXED` conversion and a `RuntimeError` shape mismatch from `NVFP4_MIXED`. Fixed by adding `safetensors_quant.layer_key()` (strips a trailing `.weight` to get the module prefix ComfyUI expects) and using it consistently for scale-tensor naming and the `_quantization_metadata` "layers" dict key in `convert_safetensors.py`. All existing FP8/NVFP4 tests updated to the corrected naming; added `test_quantization_metadata_layer_key_has_no_weight_suffix` regression test.
- **NVFP4 could corrupt ComfyUI's architecture-hyperparameter detection**: independent of the naming bug above, ComfyUI's `model_detection.py` infers hyperparameters directly from specific tensors' raw on-disk shapes before any dequantization — a pattern used across ComfyUI's supported architectures, not just Lumina2 (e.g. Flux infers `in_channels` from `img_in.weight.shape[1]`, Lumina2 infers `cap_feat_dim` from `cap_embedder.1.weight.shape[1]`). NVFP4 halves a packed tensor's on-disk last dimension (2 values/byte), corrupting that inference and crashing model load — this is a gap in this tool's non-mixed NVFP4 path (which packed those tensors indiscriminately), not a ComfyUI bug: published NVFP4 checkpoints conventionally leave small embedding/patchify layers unquantized anyway. Added `ModelTemplate.keys_shape_critical` (`models/architectures.py`) to exclude flagged tensors from NVFP4 packing unconditionally (not gated by `*_MIXED`, since this is shape-safety, not precision). Audited every architecture against a live ComfyUI install and populated it for `ModelFlux`/`ModelQwenImage` (`img_in.weight`), `ModelSD3` (`context_embedder.weight`), `ModelAura` (`cond_seq_linear.weight`), `ModelLumina2` (`cap_embedder.1.weight`), `CosmosPredict2` (`x_embedder.proj.1.weight`), `ModelHyVid` (`txt_in.input_embedder.weight`), `ModelWan` (`head.modulation`), and `ModelLTXV` (`transformer_blocks.0.attn2.to_k.weight`) — `ModelHiDream` was audited and confirmed to need no fix (its `unet_config` is hardcoded constants, no shape reads). `ModelSDXL`/`ModelSD1` remain unaudited (ComfyUI's `context_dim` detection there scans block indices dynamically rather than reading one fixed tensor name) — see `docs/issues_analysis.md` #9 for the full per-architecture table.
- `gui.py`: The **Convert → Safetensors** tab's output-path Browse button opened a file-*open* dialog (`_browse(_MODEL_TYPES)`, meant for picking a source model) instead of a directory picker. Added `browse_and_set_dst_st()` (mirrors the GGUF tab's `browse_and_set_dst()`) and `_resolve_dst_st()`, which defers the output format into the filename via a `{ftype}` placeholder so the name reflects whatever format is selected at convert time, not whatever was selected when Browse was clicked.

### Change
- `text_encoder_convert.py`: Text-encoder conversion no longer depends on a ComfyUI-Easy-Install installation. `find_convert_script()`/`find_embedded_python()` (which only searched Easy-Install roots) are replaced by `ensure_llama_cpp()`, which shallow-clones `ggml-org/llama.cpp` into `.llama.cpp/` next to this repo on first use (override via `S2G_LLAMA_CPP_HOME`), and `convert_text_encoder()` now runs `convert_hf_to_gguf.py` with `sys.executable` (this tool's own venv) instead of an external `python_embeded/python.exe`. `convert_hf_to_gguf.py` in current llama.cpp imports its ~90 per-architecture model classes from a sibling `conversion/` package rather than being self-contained, so a shallow clone was chosen over vendoring — it also stays current with upstream automatically. `pyproject.toml` gains `transformers==4.57.6`, `sentencepiece>=0.1.98,<0.3.0`, and `protobuf>=4.21.0,<5.0.0` (exact versions from llama.cpp's own `requirements-convert_legacy_llama.txt`) so the cloned script's imports resolve in this repo's venv.
- `gui.py`: Removed the "Setup: Found/Not Found" textbox from the **Convert Text Encoder → GGUF** tab — nothing to detect anymore now that llama.cpp is fetched automatically.
- `README.md` / `docs/architecture.md`: Updated Text-Encoder Conversion sections to describe the auto-clone workflow instead of ComfyUI-Easy-Install discovery.
- `tests/test_text_encoder_convert.py`: Replaced `TestFindConvertScript`/`TestFindEmbeddedPython` (Easy-Install-root-based) with `TestEnsureLlamaCpp` (clone-on-missing, reuse-if-present, git-not-found and clone-failure error paths) and an updated `TestFindConvertScript`.

### Docs
- `docs/building-llama-quantize.md`: New standalone guide for self-building a City96-patched `llama-quantize` — exact prerequisites, git tag (`b3962`), and build commands per OS (Linux, macOS, Windows/MSVC, and a no-Visual-Studio Windows/MSYS2-MinGW-w64 path), sourced from `city96/ComfyUI-GGUF/tools/README.md`. Previously the README only linked out to that upstream doc with no concrete commands. `README.md`'s "llama-quantize Sources" section now links here instead.
- `docs/architecture.md`: Added "Safetensors Output Pipeline" and "Text-Encoder Conversion Pipeline" sections (matching the existing GGUF "Conversion Pipeline" diagram style), documenting `convert_safetensors.py`/`safetensors_quant*.py` and `text_encoder_convert.py` end-to-end, including the post-final-review fixes (unconditional 1D skip for FP8/NVFP4, NVFP4 non-16-multiple fallback, lazy-streaming iteration). Replaced the stale "Future Extension: Text Encoder to GGUF" section — which still described the generic HF-to-GGUF subprocess path as unimplemented — with a "Future Work: Per-Family Text-Encoder Automation" subsection scoped to what's actually still missing (SDXL CLIP-specific extraction, Qwen `mmproj` pairing), since the generic path shipped in Tasks 7-10.
- `README.md`: Added `huggingface_hub` to the "Using pip instead" fallback install command — it was missing despite being a required dependency for text-encoder conversion.
- `gui.py`: Module docstring now mentions the text-encoder convert tab alongside GGUF convert, safetensors convert, quantize, and fix_5d.

### Fix
- Final integration review fix wave for the Safetensors-output / Text-Encoder feature branch (5 cross-task findings):
  - `safetensors_quant.py` (`quantize_tensor_st`): NVFP4 quantization of a tensor whose last dimension isn't a multiple of 16 (e.g. a 3x3 conv kernel, last dim 3 — routine in SDXL/SD1.5 UNets and some DiT patch-embed layers) now catches `quantize_nvfp4`'s `ValueError` and falls back to a plain F16 write for that one tensor, instead of crashing the whole conversion after many tensors were already processed.
  - `safetensors_quant.py` (`quantize_tensor_st`): 1D tensors (biases, norm weights) are now unconditionally excluded from FP8-scaled/NVFP4-block-scaled quantization, not just in `*_MIXED` mode. Previously non-mixed `FP8`/`NVFP4` scale-quantized 1D tensors and emitted a `<name>.weight_scale` sibling that no consumer reads (ComfyUI's scaled-quant loader only applies `.weight_scale` to a layer's `.weight`), so biases loaded back unscaled and wrong by up to ~448x. Mirrors `convert.py`'s `_quant_type_for`, which already keeps 1D tensors at F32 unconditionally.
  - `gui.py`: The **Convert → Safetensors** tab's format dropdown now defaults to `FP8_MIXED` instead of `FP8`, since mixed mode keeps hiprec tensors at F32 for extra safety margin.
  - `convert_safetensors.py`: Stopped materializing the entire state-dict into a `list()` before iterating — this defeated `load_state_dict()`'s lazy `_LazyStateDict` streaming (added in `72a49dc` specifically to bound peak RAM to ~1 tensor for >RAM checkpoints) and reintroduced the OOM/paging crash that commit fixed. Now uses `len(state_dict)` and iterates `state_dict.items()` directly, both of which `_LazyStateDict` supports without loading tensor data.
  - `gui.py` (`run_te_convert`): Cancelling the **Convert Text Encoder → GGUF** tab now renders "Cancelled" instead of a red "Error: cancelled" — added the same `RuntimeError("cancelled")` special-case already present in `run_st_convert`.
  - `README.md`: Removed the "(or `convert_safetensors.py` CLI)" / "(or `text_encoder_convert.py` CLI)" mentions — neither module has an `argparse`/`__main__` entry point; both are GUI-only features.
- `safetensors_quant_nvfp4.py`: Guard zero/near-zero blocks against NaN division. Original code relied on implicit `argmin(NaN)→0, _KVALUES[0]==0` coincidence; replaced with explicit `torch.where` guard detecting post-cast zero block scales and deliberately zeroing normalized values for affected blocks. Prevents silent correctness bugs if PyTorch's NaN-handling behavior changes across versions/backends. Added `test_zero_block_no_nan_and_reconstructs_to_zero` to cover all-zero and near-zero tensors.

### Change
- `convert.py`: Stream safetensors tensors one at a time instead of loading the full state-dict into RAM. Replaces `safetensors.torch.load_file()` with a new `_LazyStateDict` wrapper around `safe_open()` that materializes tensors on demand via `get_tensor()`. Required to convert checkpoints larger than physical RAM (e.g. the 19 GiB FP8 `Qwen-Edit-abliterated-4step-v1.safetensors` on a 16 GiB system, which previously crashed mid-conversion with `KERNEL_DATA_INPAGE_ERROR` and segfaults caused by excessive Windows paging). Source dtype detection now reads dtypes from the safetensors header instead of iterating `.values()`, so dominant-dtype detection also stays out of RAM. `.ckpt` / `.pt` / `.bin` / `.pth` sources keep the existing eager path because `torch.load` has no streaming API.
- `convert.py`: `strip_prefix` refactored to share its prefix rules with the lazy path via a new `_build_key_map(keys)` helper. Public API and behaviour for eager dicts unchanged.
- `convert.py`: `GGUFWriter` now runs with `use_temp_file=True` so converted tensors spill to a `SpooledTemporaryFile` (256 MiB RAM buffer, remainder on disk in `%TEMP%`) instead of accumulating every F16/BF16 tensor in `self.tensors`. Without this, lazy-streaming on the read side only deferred the OOM: the Qwen-Edit 19 GiB FP8 conversion progressed to tensor 1384/1933 (71%) before crashing because the writer had buffered roughly 27 GiB of F16-equivalent tensor data in RAM. Pairs with `_LazyStateDict` to keep peak RAM bounded end-to-end.

### Add
- `text_encoder_convert.py`: Bare-bones text-encoder (LLM/T5) safetensors → GGUF conversion support. Exports `find_convert_script()` and `find_embedded_python()` discovery functions that search ComfyUI-Easy-Install roots for `python_embeded/Lib/site-packages/llama_cpp/bin/convert_hf_to_gguf.py` and `python_embeded/python.exe`; reuses `quantize._easy_install_roots()` binary-discovery pattern. Defines `TEXT_ENCODER_OUTTYPES` (F32/F16/BF16/Q8_0) dropdown choices for the planned GUI. Foundation for Tasks 8–9 (config/tokenizer download, subprocess runner, and GUI integration). Bare single-file safetensors cannot be converted by `convert_hf_to_gguf.py` directly because it requires config.json + tokenizer files; planned workflow downloads these from HuggingFace + assembles a temp directory with renamed weights before invoking the embedded Python interpreter's script.
- `tests/test_text_encoder_convert.py`: Coverage for discovery functions (returns None when not found, finds scripts/pythons under Easy-Install roots), and `TEXT_ENCODER_OUTTYPES` structure (list of non-empty string tuples).
- `text_encoder_convert.py`: `fetch_base_config_files(repo_id, dest_dir, on_log=None)` — downloads a base model's `config.json` (mandatory) and best-effort tokenizer files (`tokenizer.json`, `tokenizer_config.json`, `tokenizer.model`, `special_tokens_map.json`) from HuggingFace Hub into `dest_dir`. Returns the list of filenames actually downloaded. Raises `RuntimeError` if `config.json` is missing (mandatory constraint); tokenizer files are optional and skipped individually if not found (since repos vary in which tokenizer variants they ship). Used in the text-encoder workflow to fetch supplemental config/tokenizer files when converting bare safetensors to GGUF.
- `tests/test_text_encoder_convert.py`: Coverage for `fetch_base_config_files` (downloads config + available tokenizers, best-effort missing-file handling, RuntimeError on missing config.json).
- `text_encoder_convert.py`: `convert_text_encoder(weights_path, base_repo_id, dst_path=None, outtype="f16", on_log=None, cancel_event=None)` — orchestration function that converts a bare single-file text-encoder checkpoint to GGUF. Assembles a temporary HF-style model directory with the user's weights (copied as `model.safetensors` to preserve the original) plus downloaded config/tokenizer files, then runs `convert_hf_to_gguf.py` via the ComfyUI-Easy-Install embedded Python interpreter. Streams subprocess output to `on_log`, respects `cancel_event` for graceful cancellation, and returns the output GGUF path. Raises `FileNotFoundError` if convert script or embedded python is not found; raises `RuntimeError` if the subprocess exits with non-zero code or is cancelled.
- `tests/test_text_encoder_convert.py`: Coverage for `convert_text_encoder` (raises FileNotFoundError when convert script not found, runs subprocess with expected command-line arguments).
- `pyproject.toml`: Add `huggingface_hub` dependency for base-model config/tokenizer download support in text-encoder conversion.
- `safetensors_quant.py`: Quantized Safetensors → Safetensors output backend with dtype registry (F16/F16-mixed variants) and per-tensor quantization. Reuses `convert._quant_type_for`'s high-precision-tensor rule so mixed-precision safetensors output exactly matches existing GGUF mixed-precision behavior. Foundation for Task 2–3 (FP8/NVFP4 variants) and Tasks 4–5 (convert_safetensors.py and GUI integration).
- `tests/test_safetensors_quant.py`: Coverage for safetensors dtype registry, high-precision-tensor detection, and F16/F16-mixed quantization
- `safetensors_quant_fp8.py`: ComfyUI-compatible scaled FP8 quantization for safetensors output (`float8_e4m3fn` with per-layer `weight_scale` metadata). Follows ComfyUI's scaled_fp8 convention (see `comfy/quant_ops.py` QUANT_ALGOS["fp8"] in city96/ComfyUI-GGUF).
- `tests/test_safetensors_quant_fp8.py`: Coverage for FP8 scaling, packing, dtype outputs, and round-trip reconstruction accuracy
- `safetensors_quant_nvfp4.py`: NVFP4 block-scaled 4-bit quantization for safetensors output (uint8-packed with per-16-element block scaling and global scale). Reuses `gguf.quants.NVFP4`'s E2M1 codebook table for bit-compatibility with ComfyUI's native NVFP4 loader (format reference: city96/ComfyUI-GGUF `comfy/quant_ops.py` QUANT_ALGOS["nvfp4"]).
- `tests/test_safetensors_quant_nvfp4.py`: Coverage for NVFP4 quantization (packing, scaling, dtype outputs, error cases, and round-trip reconstruction accuracy)
- `models/architectures.py`: `ModelQwenImage` — Qwen-Image and Qwen-Image-Edit DiT support (incl. 2509 multi-image edit variant and abliterated forks like `jiangchengchengNLP/Qwen-Edit-2509-abliterated`). Placed before `ModelFlux` / `ModelSD3` in `arch_list` because Qwen-Image shares `transformer_blocks.0.attn.norm_added_k.weight` and `add_q_proj.weight` with those architectures, which would otherwise trigger their reference-format `keys_banned` guard and abort conversion with "Model architecture not allowed for conversion. Use diffusers format, not reference format." Detection keys mirror upstream ComfyUI-GGUF `tools/convert.py` (`time_text_embed.timestep_embedder.linear_2.weight` + `transformer_blocks.0.attn.norm_added_q.weight` + `transformer_blocks.0.img_mlp.net.0.proj.weight`)
- `README.md` / `Supported Architectures`: Confirm FLUX.2 (klein / Dev) support via existing `flux` arch tag (shares detection with FLUX.1)
- `README.md` / `Supported Architectures`: Confirm Z-Image (Turbo / Base) support via existing `lumina2` arch tag (shares detection with Lumina 2)
- `component_extract.py`: Analyze embedded SDXL VAE / CLIP-L / CLIP-G components, compare them against local ComfyUI standard files, and export selected components
- `gui.py`: **Extract Components** tab for SDXL component analysis and optional VAE / CLIP-L / CLIP-G export
- `tests/test_component_extract.py`: Coverage for SDXL component analysis, CLIP-G Q/K/V splitting, and `text_projection` transposition
- `README.md` / `docs/architecture.md`: Document the text encoder to GGUF workflow (base repo ID requirement, ComfyUI-Easy-Install dependency, temporary HF directory assembly), encoder-family reference table (per-model encoder family and implementation status), and primary SDXL / OpenCLIP / CLIP / Qwen2.5-VL / Qwen3 / T5 / Mistral source links
- `README.md` / `docs/architecture.md`: Record a possible future checkpoint-level GGUF workflow covering component splitting, per-component quantization, mixed safetensors/GGUF layouts, and a ComfyUI loader-node direction
- `README.md` / `docs/architecture.md`: Record City96's `lcpp.patch` as a primary implementation reference for image GGUF architecture registration, tensor quantization policy, metadata bypasses, and future llama.cpp forward-porting
- `convert_safetensors.py`: End-to-end converter from model checkpoint to quantized `.safetensors` file. Sibling to `convert.py`'s GGUF writer: reuses architecture detection and state-dict loading, writes plain quantized safetensors output (no GGUF backend, no 5D side-car export, no shape_fix, no 1D-pad-token unsqueeze). Supports F16/F16-mixed, FP8/FP8-mixed, NVFP4/NVFP4-mixed quantization via `safetensors_quant.py`; includes callbacks for progress reporting, logging, and cancellation.
- `tests/test_convert_safetensors.py`: Coverage for safetensors quantized output (file I/O, dtype matching, FP8 scale tensors, quantization metadata, overwrite protection, and float8-input dtype coercion regression test)
- `gui.py`: **Convert → Safetensors** tab — GUI front-end for `convert_safetensors.convert_to_safetensors`, with source/output path pickers, an `SAFETENSORS_DTYPE_CHOICES`-backed format dropdown, and overwrite protection. The existing GGUF conversion tab is renamed **Convert → GGUF** to disambiguate the two output pipelines. Conversion runs in a background thread (`run_st_convert`, mirrors `run_convert`'s worker-thread pattern) with live-streamed log/progress via `on_progress`/`on_log` and a dedicated Cancel button/`_active_cancel_st` slot — matches the GGUF tab's design instead of blocking the UI synchronously.
- `tests/test_gui.py`: Coverage asserting the renamed **Convert → GGUF** tab and new **Convert → Safetensors** tab (with its `SAFETENSORS_DTYPE_CHOICES`-driven "Output format" dropdown) are present in `build_app()`'s Blocks graph
- `gui.py`: **Convert Text Encoder → GGUF** tab — GUI front-end for `text_encoder_convert.convert_text_encoder`, with source weights-file/base-repo-ID/output-path fields, a `TEXT_ENCODER_OUTTYPES`-backed output-type dropdown, and a read-only Setup textbox surfacing `find_convert_script()`/`find_embedded_python()` discovery results. Conversion runs in a background thread (`run_te_convert`, mirrors `run_st_convert`'s worker-thread + `_stream` pattern from the Task 5 review fix) with live-streamed log output via `on_log` (no progress bar — `convert_text_encoder` has no `on_progress` callback) and a dedicated Cancel button/`_active_cancel_te` slot, kept separate from `_active_cancel`/`_active_cancel_st` so all three conversion tabs can run and be cancelled independently.
- `tests/test_gui.py`: Coverage asserting the new **Convert Text Encoder → GGUF** tab (with its "Base model HF repo ID" field) is present in `build_app()`'s Blocks graph

## [0.1.0] - 2026-05-16

### Add
- `.github/workflows/ci.yml`: GitHub Actions CI for Windows-based `uv` dependency sync, pytest, and Ruff lint checks
- `tools/benchmark_llama_quantize.py`: Compare one or more `llama-quantize` binaries on the same GGUF source and report timing/output size
- `gui.py` / `quantize.py`: Conservative `llama-quantize` discovery for ComfyUI Easy-Install / City96-compatible binaries, plus a native file selector for choosing the executable when it is not found automatically
- `fix_pad_tokens.py`: Repair Lumina 2 GGUFs with 1D pad token shapes (`[D]` → `[1, D]`) for ComfyUI compatibility; returns output path for consistent streaming integration
- `gui.py`: **Fix Pad Tokens** tab — GUI front-end for `fix_pad_tokens.py`
- `gui.py`: K-quant pipeline auto-applies `fix_pad_tokens` after `llama-quantize` for architectures with `keys_unsqueeze` — step count and progress bar updated accordingly
- `models/architectures.py`: `ModelTemplate.keys_unsqueeze` — 1D tensor names to unsqueeze before writing; set to `["x_pad_token", "cap_pad_token"]` on `ModelLumina2`
- `models/architectures.py`: `ModelLumina2.keys_hiprec = ["x_pad_token", "cap_pad_token"]` — forces F32 storage for these BF16 pad tokens; prevents the size-doubling bug where a BF16 2D tensor `[1, D]` was stored as BF16 instead of F32 and loaded as `[2D]` (city96 issue #419)
- `convert.py`: Unsqueeze step in `handle_tensors` — applies `unsqueeze(0)` for keys listed in `model_arch.keys_unsqueeze` after dtype coercion
- `quantize.py`: `SIZE_RATIOS` dict — approximate output-size ratios relative to F16 source, used for the live size estimate in the UI
- `start_gui.bat`: one-click entry point
- `tests/test_quantize.py`: coverage for type registry, SIZE_RATIOS, Easy-Install discovery, subprocess interaction, and benchmark helpers
- `tests/test_gui.py`: coverage for `llama-quantize` Browse/Detect helper behavior
- `tests/test_convert.py`: float8 regression tests
- `pyproject.toml` / `uv.lock`: Add `ruff` as a dev dependency

### Change
- `gui.py`: Convert tab — add a description Markdown header so all three tabs share the same first-child structure; Gradio 6 sizes a tab by its first child, so the Convert tab now renders at the same width as the Fix Pad Tokens and Fix 5D Tensors tabs
- `tests/test_gui.py`: `TestLayoutParity` — assert `build_app()` returns without error and every tab has its description Markdown header
- `gui.py`: Add an Advanced thread-count control for `llama-quantize` and throttle GUI tensor log lines while keeping per-tensor progress updates
- `gui.py`: Make the `llama-quantize` path read-only; users select the executable with Browse instead of typing paths manually
- `AGENTS.md` / `README.md`: Clarify that Claude commit hooks live in `.claude/settings.json`, local permissions live in `.claude/settings.local.json`, tests run through `uv`, and `pyproject.toml`/`uv.lock` are the canonical dependency files
- `gui.py`: Page width increased from 860 px to 1100 px
- `gui.py`: Layout cleanup — all three tabs now use the same structure (card for inputs, action button below card, then status bar + log); removed `.card-actions` flex hack and `#size-info` padding tricks
- `gui.py`: Compact layout — inline Browse buttons, Quantization + size estimate side by side, Overwrite checkbox moved to Advanced, log reduced to 10/8 lines
- `gui.py`: Cancel button label shortened to `✕`; button heights reduced to 44 px
- `gui.py`: `gr.Progress()` removed from streaming handlers — it injected a UI element that called `scrollIntoView` and caused the page to jump during conversion; progress percentage is now embedded in the status text instead
- `gui.py`: `autoscroll=False` on log textboxes; `max_lines` fixed to prevent height-growth reflows
- `gui.py`: `show_progress="hidden"` on the convert event
- `gui.py`: Web UI — file browse buttons (source + output) using native tkinter dialogs
- `gui.py`: Output path auto-resolution (`_resolve_dst`) — handles directory paths, `{ftype}` placeholder, and full file paths
- `gui.py`: Quantization dropdown with 10 curated types (F32/F16/BF16/Q8_0 Python-native; Q6_K/Q5_K_M/Q4_K_M/Q4_K_S/Q3_K_M/Q2_K via llama-quantize)
- `gui.py`: Live size estimate — updates when source file or quantization changes
- `gui.py`: Cancel button (red) — signals `ConversionCancelled`, terminates llama-quantize subprocess, triggers `gc.collect()` to free RAM immediately
- `gui.py`: Scroll fix — `head=` injects a `<script>` that blocks all programmatic scroll APIs (`scrollIntoView`, `scrollTo`, `scroll`, `scrollBy`) so streaming updates never hijack the viewport
- `gui.py`: `_pipeline` step labels — dynamic total step count based on whether 5D fix and/or pad token fix are needed
- `gui.py`: Browse output folder composes full path with `{ftype}` placeholder so changing quantization after browsing always produces the correct filename
- `convert.py`: `ConversionCancelled` exception; `cancel_event` parameter on `handle_tensors` and `convert_file`; explicit `del writer; del state_dict` on cancel to release RAM
- `convert.py`: Float8 dtype coercion before `nan_to_num` (regression fix — `nan_to_num` does not support `float8_e4m3fn`)
- `quantize.py`: `cancel_event` parameter on `run_quantize`; `proc.terminate()` on cancel
- `fix_5d_tensors.py`: `on_progress`, `on_log` callback parameters

### Fix
- `tests/test_gui.py`: Make the non-Windows `llama-quantize` guidance test portable on Windows CI by avoiding patched `os.name` path construction
- `gui.py` / `convert.py`: Keep Lumina 2 pad tokens 1D in K-quant intermediates for compatibility with the patched Easy-Install `llama-quantize`, then re-apply the pad-token shape fix after quantization
- `.claude/settings.json`: Use a repo-local pytest temp directory and disable pytest cache in the commit test hook to avoid Windows temp/cache permission failures
- `gui.py`: K-quant Lumina 2 conversions — `llama-quantize` collapsed `[1, D]` pad token shapes back to `[D]`; pipeline now re-applies `fix_pad_tokens` automatically after quantization
- `convert.py`: `nan_to_num` crash on `float8_e4m3fn` tensors (Lumina2 models) — coerce to float16 before clamping
- `gui.py`: Output filename used stale quantization key when user changed quant after clicking Browse — now uses `{ftype}` placeholder resolved at conversion time
- `gui.py`: Step counter showed `[2/?]` — now shows correct total based on which post-processing steps are needed

---

<!-- Format:
## [x.y.z] - YYYY-MM-DD
### Add
### Change
### Fix
### Remove
-->
